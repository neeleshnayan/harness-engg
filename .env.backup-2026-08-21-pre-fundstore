ALPACA_API_KEY=PKG4PQVHWJWWSD5PTPW6YC3RE5
ALPACA_SECRET_KEY=HYSqWUBkiDVpTxjSwdq4N3ZffNc9UDYmALCSgqzGQbbR
ALPACA_PAPER=true
# Local ledger by default. This was =0, which meant any bare `uvicorn app.main`
# — a debug restart, a script, a session-continuation like tonight's — silently
# attached to PRODUCTION Firestore and served its stale book as the truth while
# the real trading history sat in the local file. Production is an explicit
# choice: scripts/run.sh sets these, nothing else should.
USE_FAKE_FIRESTORE=0
FIREBASE_SERVICE_ACCOUNT_JSON=firebase_service_account.hedgefund.json
FUND_ENV=staging
FIRESTORE_DATABASE_ID=default
# 300s meant a fill could sit unrecorded for five minutes: NAV wrong and
# the risk engine evaluating a book that no longer existed. Stopgap until
# the trade_updates websocket replaces polling entirely.
SETTLE_INTERVAL_SECONDS=20
STRIKE_INTERVAL_SECONDS=3600

# Gate for /fund/signals/external (LEAN sidecar). Unset = intake OFF.
EXTERNAL_SIGNAL_TOKEN=siaObAmPspscWu0j_KY7vDaE7bv7CCu3ElRp1p_7-TE

# Polygon.io free tier: adjusted daily OHLCV + vwap, ~2 years of history,
# and delisted-ticker reference data. Hard limit 5 requests/minute.
POLYGON_API_KEY=xfMhtwCre_s0pKyLFvoqqr4zHp0OWpwM
