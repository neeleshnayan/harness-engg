fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
firebase-admin>=6.5
python-dotenv>=1.0
# Live venue (Step 2) — paper or real trading via Alpaca
alpaca-py>=0.20
PyPortfolioOpt
# Thesis generation clusters evidence with TF-IDF + agglomerative clustering.
scikit-learn>=1.4
psycopg[binary]>=3.1
