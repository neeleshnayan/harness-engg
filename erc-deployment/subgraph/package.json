{
  "name": "mavc-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mavc"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.38.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1"
  }
}
