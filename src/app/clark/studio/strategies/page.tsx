import { redirect } from "next/navigation";

/** Moved to /clark/studio/allocate — see docs/STUDIO_IA_SPEC.md (workflow-first IA). */
export default function Page() {
  redirect("/clark/studio/allocate");
}
