# HANDOVER CHARTER — Fable OOO (CEO-granted, 2026-08-24 ~14:05Z)

**Written by the Fable chair for the co-CTO (Opus).** CEO instruction,
verbatim: *"you have been working very hard and have earned a day off.
Prepare a charter for next items and let co-CTO take care while you are
OOO."* Your standing charter (`co-cto.md`, the constitution's three tiers)
binds unchanged; this file is the DAY-SPECIFIC work list on top of it.
Read order on cold start is unchanged: co-cto.md → DAY_LOG.md →
CTO_REVIEW_QUEUE.md → this file → cto.md (read-only).

## ⚠ SESSION-BOUNDARY WARNING, FIRST

Two builder agents are running INSIDE the Fable session's process
(highway slice 1 in ClarkHarness worktree `scratchpad/hw1`; KP desk polish
in `scratchpad/kpp`). **If that session's process is killed before they
return, they die with it** (this morning's rewind killed three the same
way). They commit-as-they-go, so losses are bounded — but check their
worktrees for committed work before re-dispatching anything. If their
bundles exist (`hw1.bundle` / `kpp.bundle` in the session scratchpad),
they finished.

## A. MID-FLIGHT AND TIME-CRITICAL — R39 (Monday execution, in progress)

State at handover: sync DONE (seq 1414, CEO's own click, NAV $2,011 → all
in sync). Probe DONE (fill verified at broker). Five of six orphan sells
FILLED (GLD 427.48 / XLE 63.54 / SOFI 18.58 / MSFT 484.77 / NVDA 210.20).
NAV struck post-sync (the mark-sanity lesson — see cto.md, today's
last section). **INTC sell awaiting the CEO's re-click** (guard satisfied
after the strike; order `5e05740e`).

1. **Verify the INTC fill** at the broker after the CEO clicks (event log
   OrderFilled + venue cash moves ~+$134).
2. **Phase 4 at 14:30–15:00Z**: run
   `ClarkHarness/scripts/r39_phase4_rebuys.py` (repo venv python). It
   verifies each exit rule live and skips uncovered symbols by itself.
   Four CEO clicks follow on the Monitor. Tier 2: record the staging in
   the review queue as `neelesh-via-co-cto` execution of the CEO-accepted
   R39-4. NBBO capture is inside the script.
3. **Phase 5 acceptance by 15:30Z**: `/fund/venue/reconcile` must read
   `symbols_out_of_sync: 0` AND `|delta_usd| ≤ 3.00`. An UNSOURCEABLE
   residual > $10 = STOP EVERYTHING, fresh-account trigger 2, park for
   Fable + tell the CEO. Record the acceptance (or the stop) in the day
   log and the review queue.
4. **After Phase 5 passes**: (a) the 10-symbol ENTRY FREEZE lifts
   automatically (it keys on reconcile in_sync — verify it reads lifted,
   note it); (b) **restart the spine** (Tier 2 — follows from the merged
   D36..D41 stack + the durability fix; both are already merged on the
   live tree, suite 4532 green). One restart, then verify
   `/fund/health` 200 and `/fund/desk` serving. (c) NAV strike again
   post-Phase-4 so the rebuilt sleeve carries marks (today's lesson:
   sync/trade → strike, always).
5. **~15 min after US close (~20:15Z), if a session is live**: run the
   retro spread capture per the validator's Monday list
   (`scripts/` — retro_spread.py `--quotes --store`). If no session is
   live at that hour, note it in the day log; do not fake the timestamps.
6. **EoD**: Donna's dispatch (standing authorization, memo + desk-hygiene
   section), then the pg dump (local only — NEVER push dumps; GH001).

## B. BUILDER RETURNS — resolve pipeline, with one carve-out

When the two in-flight builders return (or their bundles are found):

- **KP desk polish (`builder-kpp`)**: full resolve pipeline is yours
  (verify claims, file run record, STATE verbatim, BINDS carried).
  MERGE allowed under your Tier 2 IF the KP suite + tsc + next build are
  green on the merged tree AND the diff stays inside
  `src/app/clark/studio/desk/**` (16 pre-existing tsc errors in
  `src/app/clark/next/**` are known and NOT theirs). The CEO is waiting
  on the loading-vs-unreadable fix — merge-worthy today if green.
- **Highway slice 1 (`builder-hw1`)**: resolve pipeline yours. The MERGE
  splits: the ticket-fold commit (read-only, `app/fund/tickets.py` +
  GET endpoint) and the grammar commit are Tier-2 mergeable if green.
  **The lamp-door commit (touches `_refuse_unknown_request`, an
  approval-path door) is TIER 3 — park the bundle, queue entry, Fable
  merges.** Record the slice-1 reconciliation number loudly (fold counts
  vs desk_load) — it is the baseline for every later slice.
- Two new pending desk items the CEO flagged live today, already
  ticketed/known: the Orders panel labels unapproved orders
  "PENDING · in flight" (falsely claims venue presence — next KP
  dispatch), and refused approvals render as a dead button (same
  dispatch; refusals must render loudly at the click site).

## C. ALLOWED WITHOUT WAITING (your Tiers 1–2, day-specific)

- **Quant re-queue, off-peak (after 20:00Z)**: the Entry-20 deployable
  non-fractional arm (nav=$500, fractional=0) timed out at 900s this
  morning (job 95c313a9b403). Re-submit ONE belt job, longer timeout if
  the belt supports it. Experiment lane (Delegation v1 applies to the
  chair; for you it is Tier 1 dispatching under standing rules). Do not
  interpret the three-arm sweep — file results, Fable's quant dispatch
  interprets.
- Desk hygiene sweeps with citations (use
  `ClarkHarness/scripts/desk_sweep.py` — it refuses closures without
  citations by design).
- Recording, filing, STATE/BINDS appends, day-log upkeep — all Tier 1.

## D. DEFERRED TO FABLE — park in the queue, never execute

1. **The register apply + signing** (`min_psr_pct` why/falsified_by from
   the now-repaired draft) — register changes are CTO-chair acts.
2. **Highway slice 2+** (doors, generalized phantom guard, decision_ref
   409s — approval-path; adversary-blind first, Fable dispatches).
3. **D41-N1 gate repair dispatch** (gate code = Tier 3).
4. **The lamp-door commit merge** (B above).
5. **Anything touching**: guard, autopolicy, gate values, risk, exit
   mechanics, event store, thresholds, constitution. As always.
6. **COO triage #9**: the CEO personally stopped it mid-run today. Do NOT
   re-dispatch unless he asks.
7. **Vault pushes**: manual mode is on; commit locally, push only on the
   CEO's word, only to the three-branch vault surface.

## E. WHAT THE CEO STILL OWES THE DESK (do not nag, just keep visible)

INTC re-click (now) · four Phase-4 clicks (~14:30Z) · the ticket-highway
aging policy (nothing expires until set) · P-2 routing decision · the
seat-page spend-tiles decision (D42 rec 2) · Stan's gold batch
(G1/G2/G3/G5/G8) · E20-1 · AB4-2.

## F. FABLE'S RETURN CHECKLIST (self-note)

Review queue verification → Tier-3 decisions (register apply first) →
lamp-door merge → slice-2 dispatch with adversary pre-review → D41-N1
brief → premia re-belts on the restarted spine's schema-4 → the
follow-the-day read of Donna's archive.

— Fable, 2026-08-24 ~14:05Z. The record never lies; keep it that way.
