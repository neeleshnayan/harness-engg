# COO triage memo #2 — 73 items → 10 truly open → 7 batches

**Author: coo (Vishesh), triage dispatch #2, 2026-08-20T18:54Z — fired by
the CTO under the CEO's standing >20 rule on desk_load's first live
reading. Filed verbatim by the CTO at resolve; CTO note at the bottom.**

**Headline: the desk says 73. The true count is 10.** `desk_load` counts
`open` + `accepted` + `staged` (deskstore.py:247 via desk.py:379) — 57 of
the 73 are decisions the CEO already made. True CEO-desk load = 10 open +
0 pending orders + 0 requests = **10, under the trigger of 20**. This
dispatch still earned itself (five findings, one a reversal of the COO's
own founding memo), but the next one should fire on a real count.

**The book, verified at 18:53Z**: NAV $1,884.98 · cash $1,383.46 ·
positions $501.52 (DBC $252.76, TLT $248.76) · gross 26.61% · drawdown
7.43% of 10% · halted since 15:52:31Z (daily_loss 6.30% vs 4.00%) · zero
pending orders. **No dollar on this desk moves tonight.**

## The batches, in decision order

**A — THE HALT AND THE $1,383.46** (1 item; biggest money, cheapest
decision). ENDORSE: do nothing until 2026-08-21T00:00Z, then resume — at
00:00Z the loss reference rolls to the 08-20 close ($1,884.55) against NAV
$1,884.98 = **+0.02%**, the alarm clears by arithmetic, and a resume
sticks (resume earlier and riskmonitor.py:845-855 re-halts within a tick).
**OBJECT to using the new rebase route tonight**: it would work, but it is
the control for needing to trade *inside* the day you lost; spending it to
skip five hours of waiting teaches the desk it is a convenience. Nothing
needs to trade: zero pending orders, alpha sleeve $0, sells never blocked.

**B — THREE INSTRUMENTS THAT OVERSTATE** (3 items + 1 new; $0 today).
(1) **Self-reversal**: the COO's founding memo deferred sleeve
falsification D5 on the reasoning that four fills would reach the 20-fill
TCA bar — wrong: all four filled on the PAPER venue at execution_bps −0.0
(zero by construction). ENDORSE PM R5 as originally written: record D5
UNTESTABLE-TODAY. (2) The CEO's EXECUTION QUALITY panel
(ExecutionQuality.tsx:93-114) reads the top-level TCA summary, not
`summary.informative` — it shows "20 fills / 0.0 median / 2.47bps" when
the informative reading is "10 fills / 3.53 / 5.56bps vs 5.0 assumed,
reliable:false". Point it at the informative block. (3) NEW: `desk_load`
itself counts decided items — fix to `status=="open"` only, noting this
cuts the COO's own dispatch frequency.

**C — THE GATE v5 / HISTORY PACKAGE** (4 items as ONE versioned change).
ENDORSE all four validator wfgeom recs as one package, ratifying the BLOCK
first (it holds in fact but not on the record — the register still shows
the old floor and `drifted: false`). r3 (define `available`) is
load-bearing; r1 (coverage qualifier) and r4 (persist the fold table) are
not yet shipped.

**D — TWO CONTROLS WITHOUT A SURFACE** (3 items). `GET /fund/autopolicy`
is 404 — the envelope the constitution names as the human governance seam
(live: v3) has no read surface; build it first. CLAUDE.md still documents
the v2 envelope (stale doc, NOT a loosening — v3 is tighter and
versioned); correct the text. `cross_checks()` gained a caller but only as
except-pass telemetry, not a decision path — the phantom-price mechanism
class with a nicer readout; and record the controls-or-decoration answer
in the register.

**E — THE DESIGN DECISIONS** (4 items; CEO's call, no money). The CDO's
emerald item and the builder's colour item are the SAME decision filed by
two seats — decide once (nothing has been versioned; verified by grep).
DEFER the 3D floor: largest discretionary spend on the desk, nothing
depends on it. Review or explicitly adopt the reversibility table.

**F — MARK DONE / SUPERSEDED** (54 items, one sweep, no reading): 52
verified executed against commits and the live spine; 2 superseded
(pm-review r1's trim — gross is 26.61%, answered by the retirements;
mechanism r4's cost measurement — refused structurally, replaced by the
slip-band route). Caveat: `money_at_stake` shipped on the write path only;
0 of 73 legacy recs carry it — every future run must populate it (this
run record does).

**G — THE WORK QUEUE** (4 items; cost, not risk). Rank first: **the
slip-band belt run for entry-11 at {1,3,5}bps — the only item on the desk
that can produce a new candidate.** Then round 4's blind adversary attack
(closes six adversary recs as one decision), then funnel cycle 2 (entry 8,
mechanism + analyst). Riskofficer F4 and DECISIONS_PER_TEST_LEG stay
honestly-open measurement debt.

## Boundaries and conflicts, stated

No web (charter); gate/belt numbers cited from the validator's and
adversary's measurements, not re-derived; CDO design claims verified by
grep and commit, not screenshots. **Liveness reads all six jobs `ok:
null` in this process** — a scheduler holds the lease elsewhere; its one
live consequence is that auto-execution is failed-closed twice over
(halted + liveness), so every envelope debate is $0 operationally today.
Conflict: `run-coo-1 r1–r8` are the COO's own — status reported, r6
corrected, none re-endorsed.

---

## CTO note (2026-08-21, at resolve)

Three claims verified before acting: deskstore.py:247 counts
open+accepted+staged (exact), `GET /fund/autopolicy` 404s (exact),
ExecutionQuality.tsx reads `tca?.summary` not the informative block
(exact). **Batch B item 3 was fixed the same hour** — desk_load now
filters to truly-open (rows with no status still count: dropping a
malformed row would hide work; pinned by test with the 3.65× incident in
its docstring; live reading moved 73 → 16 with `coo_triage_due: false`).
The self-reversal on D5 and the objection to convenience-rebasing are the
seat doing exactly what it was hired for. Batches A, C, D, E, F, G await
the CEO. Recorded as run-coo-2.
