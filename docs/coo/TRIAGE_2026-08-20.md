# COO TRIAGE — the CEO's desk, 2026-08-20

**Author: coo agent, founding dispatch (f35c4fad / trace f35c4fad-bcae-4509-a1cd-7272da8b065a),
spine read 10:21–10:44Z. Filed verbatim by the CTO at resolve; CTO verification
note at the bottom. 20 open items → 6 decisions + 1 non-decision.**

## The desk, counted

| Source | Items | Status |
|---|---|---|
| `/fund/orders/pending` | 4 sell tickets, $634.55 | expire **12:21:40Z** |
| `run-pm-staging` R2–R7 | 6 | open |
| `run-adversary-v5r3` recs 1–6 | 6 | open |
| `run-riskofficer-1` R5 | 1 | open |
| `/fund/desk.requests` | 2 build/proposal asks (8804b28d, 2f0959d6) | open |
| `/fund/judgement` | 1 DRIFTED entry + 2 fired review triggers | unowned — no rec exists |
| `/fund/risk/monitor` | `halted: true` | see §0 |

`/fund/rebalance/pending` `[]`, `/fund/memos` `[]`, `/fund/lp/*` no holdings.

## §0 — The one item that needs NO decision today

**HALT RESUME — DEFER.** Resuming now is undone within ~6 seconds:
`riskmonitor.py:575-581` auto-halts on any critical `daily_loss`/`drawdown`
alarm, and the 6.62%-vs-4.00% daily-loss alarm is live with the monitor ticking
at 6s. `riskmonitor.py:441-448` measures against the PRIOR-DAY strike, so the
alarm clears when the UTC date rolls (≈13h), not when you click. And nothing on
the desk needs the resume: `pipeline.py:72` blocks **BUYs only** — all four
pending tickets are SELLs and approvable while halted. **Leave halted; resume
after 2026-08-21T00:00Z once the alarms list is empty.**

## BATCH A — Close the orphan book (4 sells + PM R7) · $634.55 · expires 12:21:40Z

SOFI $169.25 · NVDA $163.15 · XLE $174.84 · SPY $127.31. Gross $1,135.09 →
$500.54 (60.42% → 26.6% of NAV), leaving exactly the sleeve. The deciding
fact: `/fund/exits` returns **9 rules, none on SOFI/NVDA/XLE/SPY** — 33.8% of
NAV held with no committed exit and no live owning strategy. **ENDORSE all
four + R7.** The fork stated plainly: XLE (+3.10%) and SOFI (+1.40%) are
*profitable* orphans — keeping them requires explicit written CEO adoption
plus the PM's §2 rule set; a split (sell NVDA+SPY, adopt XLE+SOFI with rules)
is the only alternative I would sign. **Material correction: PM R7's central
caveat is obsolete** — autopolicy v2's `rule_predates_position` means a rule
committed today on a 2026-08-14 position CANNOT auto-execute; it raises a
proposal that waits for a click. Free measurement: four fills takes TCA from
16 to exactly the 20-fill bar and discharges sleeve falsification D5. These
tickets already expired once; a second lapse costs another restaging cycle.

## BATCH B — Envelope v3: the ownership hole (riskofficer R5 + PM R4)

**R5 ENDORSE** — but its urgency is spent: verified live, only the sleeve's
TLT ($250.71) and DBC ($249.84) rules can pass `rule_predates_position` today,
and `sleeve_beta_500` owns both. **R5 protects $0 today**; its value is the
next test-harness rule. **PM R4 ENDORSE, re-scoped from live-risk to record**:
the GLD rule already fired (08:01:26Z), `exitrule.py:275-278` skips triggered
rules, GLD qty is 0 — inert, not armed. But `exitrule.py:181-183` clears
`triggered` on re-commitment, so record the override ("test artifact, not
portfolio policy") so it cannot be silently re-armed. **One decision: cut
envelope v3 = v2 + R5, overriding the GLD rule in the same versioned change.**

## BATCH C — The register's three unowned questions · CONSTITUTION CHECK

1. **`MAX_CONCURRENT_CONTAINERS` — registered 1, live 6** (`leanrunner.py:83`,
   env default). **OBJECT to leaving it**: this is the one forbidden move
   (quiet loosening), reached the desk only because the register flagged it.
   Re-register 6 with a written reason, or revert to 1. Sixty seconds.
2. **PM R6 ENDORSE — and it is not discretionary**: the register's own
   review trigger on `min_effective_bets` ("first drawdown episode over 3%
   from peak") **has fired** at 7.75% live.
3. **`max_drawdown_pct`**: trigger text says "currently 3.3% utilised" against
   a live 77.5%. Record "watched, unchanged" and refresh the stale text.

## BATCH D — Gate v5 round 4 · SPLIT 5+1

Recs 1, 3, 4, 5, 6 **ENDORSE as one round-4 brief** (no skew-blind premia
criterion; state the generator's unmeasurable-fold rate; class-maximum
drift hole; reconcile §3.1/§3.4; fix the taxonomy). **Rec 2 SPLIT — the
sharpest item on this desk**: **OBJECT to `FUNNEL_2026-08-20.md` §3 as
written** — it lists the 10y history backfill as prerequisite #1, which trips
`min_walkforward_folds`' BLOCKING register review (measured gate FP 2.9% →
12.5%). Executing #1 before the fold-scaling review lands is a 4.3× quiet
loosening. **Re-order the funnel's prerequisites and accept rec 2 — BEFORE
Batch F fires the funnel.**

## BATCH E — The sleeve record: finish what R1 decided · $0

**R2, R3 ENDORSE** (the TLT/DBC thesis notes still read "answered by a human
at every review" — the design the CEO removed, still live in `/exits/check`).
**R5 SPLIT**: D6 ENDORSE (no broker equity — simulated venue — record as
untestable before the 2026-09-08 time exit); **D5 DEFER onto Batch A** — if
the four sells fill, D5 is discharged, not untestable.

## BATCH F — Ignition (both queued dispatches) · ~$5/week

**8804b28d ENDORSE, fire** (exec desks + C1/C2/C3; C1 re-verified live:
$810.21 = 43.13% of NAV rendered as "0.0% deployed · every strategy near
target"; note Batch A shrinks the symptom to ~9% while the structural defect
remains; carry builder rec 8's dead-spine + screenshot process).
**2f0959d6 ENDORSE, fire — but only after Batch D rec 2.**

## The order to decide in

1 — **A** (money + the only clock) · 2 — **D rec 2** (measured cost of
inaction) · 3 — **C** (constitution, minutes) · 4 — **B** (control change
outranks features; $0 blast radius today) · 5 — **F** (after 2) · 6 — **E**
($0) · 7 — **D rest** (rides with the next gate dispatch) · — **halt: not a
decision today.**

*"If you say yes to all of Batch A without pausing on XLE and SOFI, I built
that batch wrong — those two are green, and 'sell a winner because its parent
failed the belt' deserves the twenty seconds."*

## What I did NOT review

The adversary's arithmetic (the validator's lane, not mine); PM's §1c
post-trade replication (I do not call POST endpoints either); the KryptonPay
briefs and diffs (merged, no open decision); nine accepted-but-unqueued items
needing CTO scheduling, not CEO decisions (confirmed one still unbuilt:
`GET /fund/autopolicy` → 404); fees/ledger/reconcile/custody/history/signals
(nothing pending); the four orphaned factory candidates (CTO hygiene). No web,
per seat boundary.

---

## CTO verification note (2026-08-20, at resolve)

Spot-checked before filing, all CONFIRMED: (1) `leanrunner.py:83` —
`MAX_CONCURRENT_CONTAINERS = int(os.getenv("LEAN_MAX_CONCURRENT", "6"))`
against a register entry of 1: the drift is real and unowned; (2)
`pipeline.py:72` — the halt blocks BUYs only, exactly as the §0 non-decision
requires; (3) the funnel-ordering objection — sustained and FIXED the same
hour (FUNNEL_2026-08-20.md §3 reordered, gate work now gates the data work).
The memo's re-scoping of PM R4/R7 on autopolicy v2 grounds matches the v2
implementation I shipped (commit 441d04c). The COO's run_record envelope was
posted verbatim as run-coo-1.
