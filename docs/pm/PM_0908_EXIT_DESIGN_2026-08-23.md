# PM — the 2026-09-08 exit design, and Entry 20's binding leg (corrected twice)

**Filed verbatim by the Fable chair from run `run-pm-0908`, 2026-08-23. Chair
verification before filing, all line-exact: `pipeline.approve_order`
(`pipeline.py:230-282`) performs staleness → quote → execute with NO venue,
compliance, or risk re-check; the autopolicy decline is a bare
`logger.warning` (`autopolicy.py:719-723`) whose return `main.py` discards;
`exitrule.py:298-302` skips any rule carrying `triggered_at` forever;
`exitrule.py:326` raises `Order(venue="paper")`. STATE appended; BINDS
carried; EVOLVE applied. R41+R42 folded into the hazard batch (rank 1); R37
routed to the adversary (direction ambiguity, PM self-flagged); R47 ticketed
for the quant; R38/R39/R40/R43/R44/R48 are the CEO's.**

---

```
TL;DR
```
**The fund is running two different portfolios and only one of them owns securities.** The book holds TLT/DBC/SPY/DBA worth $917; the broker holds GLD/INTC/MSFT/NVDA/SOFI/XLE and a partial SPY. Every exit rule on the book — all eight — would be refused at the broker, so stated exit coverage is 100% and executable exit coverage is zero.

**On 2026-09-08 two of those exits fire and die permanently.** They will be declined (correctly), the proposals expire in two hours, and the rules mark themselves used-up with no way to re-fire. $501.58 loses its time exit silently. Worse: if anyone clicks approve in that two-hour window, the order opens a $501.58 short, because the broker holds zero and shorting is on.

**Two cheap clicks remove the whole hazard** (R37: re-decide the exits in writing before the date; R41: make a refused exit visible on your desk). The expensive question — bringing the broker and the book back into agreement, ~$1,097 of legacy to sell and ~$750 to buy — needs a written plan first, not an order.

**Entry 20: your PM was wrong twice, not once.** PDT is gone as a constraint (confirmed in our own code), and so is granularity — our universe is fractionable-only by construction. The real blocker is that Entry 20's edge was measured on a $10M whole-share book and nobody has measured it at the $250 we could deploy.

---

## 1. The book in one table

`/nav` live == `/nav` last_struck (2026-08-21T20:39:12Z) — marks frozen, market closed. **NAV $1,885.74** = positions $917.06 + cash $968.69.

| Position | Qty | Mark | Value | Weight | Unrealised | Exit rules (book) | Executable at broker? |
|---|---|---|---|---|---|---|---|
| SPY | 0.346119 | 765.55 | $264.97 | 14.05% | +0.39% | loss 7.3% · time 2026-11-19 | **NO** — broker holds 0.217757 |
| DBC | 8.122157 | 31.25 | $253.82 | 13.46% | +1.53% | loss 8.7% · **time 2026-09-08** | **NO** — broker holds **0.0** |
| TLT | 3.019871 | 82.045 | $247.77 | 13.14% | −0.89% | loss 4.0% · **time 2026-09-08** | **NO** — broker holds **0.0** |
| DBA | 5.314306 | 28.32 | $150.50 | 7.98% | −0.21% | loss 6.1% · time 2026-11-19 | **NO** — broker holds **0.0** |

**Exit coverage as the D14 fix now reports it:** `coverage_known: true`, `uncovered: []` — book coverage genuinely 100% and genuinely readable, first review without a caveat. **And it is the wrong number to act on: executable coverage is 0 of 8.** Every live rule sells a quantity the broker does not hold.

**The broker's own book** (broker_equity $2,012.28, 10 of 11 symbols out of sync, delta $126.54 / 6.71%): GLD $179.70 · SOFI $173.75 · XLE $175.00 · MSFT $164.33 · NVDA $161.02 · INTC $144.90 · SPY(partial) $166.74 = **$1,165.44** — exactly the pre-T1–T8 legacy book the CEO ordered closed on 2026-08-20. The book recorded those closes; the broker never received them (every order since executed on the `PaperConnector` — the D11 venue forgery, fixed tonight).

**Capital actually deployed under mandate is $166.74 — 8.8% of NAV — not $917.06.** Leg 3 of the team metric, measured, at a quarter of what the record claims.

## 2. Mandate check (headlines)

Drawdown 1.17%/10 (rebased peak 1908.09). **Gross 48.6313% vs throttle 48.0802% — BREACHED +0.55pp/$10.39, third review, and `throttle.apply_to` still has zero production callers.** Cash floor fine ($874.40 idle above it). Effective bets 4.01. ES97.5 1.218%/$22.96. Max position SPY 14.05%/20. `nav_strike`+`snapshot` UNOBSERVED, fifth review. **The binding constraint on this book: no control on it can execute** — position limits and drawdown halts are irrelevant to a portfolio whose exits route to a broker that holds none of it.

## 3. Exceptions (new)

**E5 — the 2026-09-08 exits do not "self-close"; they self-destruct.** The chain, fully sourced: rules fire → `exitrule.py:326` raises `Order(venue="paper")`, a string mode now IGNORES for routing (`permitted_connectors=("alpaca",)`; `pipeline.py:282` executes `self._connector`) → propose-time gates PASS (alpaca `validate` checks qty>0+symbol only; compliance warns on sells only when `shorting_enabled is False`, and the broker returns **true**) → **autopolicy v4 correctly DECLINES** (`venue_holds_position`, `book_venue_in_sync` vs 1e-6 drift) → **the decline is a bare `logger.warning`; `main.py` discards the return — no event, no alarm, no desk row** → `EXIT_RULE_TRIGGERED` is appended unconditionally, stamping `triggered_at` → proposal expires at 120 min → **`exitrule.py:298-302` skips the rule forever; only a fresh `EXIT_RULE_SET` re-arms.** Net: after 2026-09-08, $501.58 holds with no time exit and no mechanism that can re-arm one, silently.

**The branch nobody had priced: a HUMAN clicking the proposal inside the 120-minute window bypasses every v4 check** — `pipeline.approve_order` does staleness → quote → execute, with no venue, compliance, or risk re-check — and **opens a $501.58 short** (broker holds zero; shorting enabled), taking the book-vs-world gap from $126.54 to ~$628.

**E6 — sleeve falsification condition #3 is TRIPPED** (`SLEEVE_500_FRAMEWORK.md:53-55`: NAV vs broker equity beyond a stated tolerance): measured 6.71%, 10 symbols. No NAV-level tolerance was ever stated — close that forward-only (R44), never by picking a number today that sits above 6.71%.

**E7 — falsifier #1 would report PASS on the day the mechanism breaks** (a closing proposal DOES appear; the failure is one layer down). Lesson for sleeve v3's pre-registration.

**E8 — broker cash is unreadable through the fund's own instruments** (`AccountState` has no `cash`/`buying_power`; `alpaca.py:351-360`). A $750 buy programme cannot be planned against an unreadable balance (R42).

**E9 — the decision register will be silent on 2026-09-08** (`sleeve_horizon_days`: `trigger_spec: []`, `due: false`, "no reader wired"). The evaluability fix has not landed.

**E10/E11 — standing:** TCA unchanged (honest n=3; `venue_disputed: 1`); three live sleeves at `claim_type: null` holding $917.06.

## 4. Recommendations (each one decision; R37+R41 remove the whole hazard for ~$0)

- **R37** — Re-decide the 2026-09-08 exits IN WRITING now: record `EXIT_RULE_OVERRIDDEN` on (sleeve_beta_500, TLT, time) and (…, DBC, time), reason "broker holds zero; the exit cannot execute and would open a short if clicked." Converts a silent self-destruct into a recorded human decision and closes the accidental-short branch. **Direction flagged by the PM itself**: mechanically an override (disarms an exit), substantively tightening — routed to the adversary blind. $501.58 · CEO · due 2026-09-05 · reversible (fresh `EXIT_RULE_SET` re-arms).
- **R38** — Close the six legacy positions the broker still holds ($998.70 + SPY excess $98.29 = $1,096.99). Not a new decision — T1–T8, decided 2026-08-20; the broker never got it. Gated on R39 (the book recorded those closes at 2026-08-20 marks including the phantom GLD $100.00 — the difference is the phantom's shadow and must be recorded, not absorbed). $1,096.99 · CEO · due 2026-09-05 · irreversible.
- **R39** — Written per-symbol reconciliation plan BEFORE any reconciliation order: which ledger moves, what event records it. Reconciling positions does not reconcile equity — the divergence GROWS to ~$225 on the PM's estimate (an estimate: broker cash unreadable). $126.54 governs $1,847.36 of turnover · CEO · due 2026-09-05 · reversible.
- **R40** — Buy TLT/DBC/DBA/SPY at the broker to match the book ($750.37 at book marks) — the act that makes the stated deployment real and every book exit executable. Gated on R39 + R42; note R48. $750.37 · CEO · due 2026-09-05 · hard.
- **R41** — Make an autopolicy DECLINE a first-class record (event/alarm with failed check names), not a discarded log line. On 09-08 the fund's own exit will be refused and nothing the CEO can see will say so. TIGHTENING, additive. $917.06 exposure · chair · due 2026-09-05 · reversible. **[FOLDED INTO THE HAZARD BATCH]**
- **R42** — Add `cash` and `buying_power` to `AccountState`. Gates R40. chair · due 2026-09-05 · reversible. **[FOLDED INTO THE HAZARD BATCH]**
- **R43** — Record sleeve falsifier #3 as TRIPPED: the sleeve's honest conclusion is "the loop completed on six of eight legs and failed on accounting." $501.58 · CEO · due 2026-09-08 · reversible.
- **R44** — Set a NAV-vs-broker-equity tolerance, FORWARD-ONLY, no number proposed (picking one with 6.71% on the board is the quiet move the constitution forbids). CEO, versioned · due 2026-09-08 · reversible.
- **R45** — Strike PDT from the graduated-sizing MIN (verified in our own code: ip+3 generates zero day trades; `/compliance` returns nulls). LOOSENING of a work-layer design doc, named; the ComplianceGate block stays armed pending the adversary. chair · due 2026-08-29 · reversible.
- **R46** — Replace "staggered entries to survive PDT" with a scheduling rule: no same-session opposite-side fill in the same symbol; defer colliding entries one session. No cash drag (unfilled slots = EW universe); survives whatever the adversary decides on PDT. chair · due 2026-08-29 · reversible.
- **R47** — **Re-run Entry 20's belt at nav=250 and nav=500 with the `FRACTIONAL_PARAM` opt-in before any sizing.** Granularity does NOT bind (the universe is fractionable-only by construction, `universe.py:119`; the tick floor is a size-invariant bps quantity) — but every belt run so far was WHOLE-SHARE at $10M (`honours_fractional()` False; `main.py:372` set_cash from nav param). At $250/40 slots, $6.25 buys 0.071 shares of an $88 median name = zero whole-share. The fund measured the analogous error at 15pp. Still UNMEASURED: Alpaca's minimum fractional notional — one probe. $250 · chair→quant · due 2026-08-29 · reversible.
- **R48** — Name THROTTLE ROOM the top row of the binding-leg MIN today: gross is $10.39 OVER before any new deployment, and nothing enforces the throttle (`apply_to`: zero production callers). Any Entry 20 pilot is a written throttle excursion the CEO signs. $10.39 · CEO · due 2026-08-29 · reversible.

## The corrected binding-leg MIN for Entry 20

| Rank | Leg | Value today | Status |
|---|---|---|---|
| 1 | **Throttle room** | $0 — book $10.39 over | MEASURED, binding now |
| 2 | **Belt validity at deployable size** | unknown — edge measured at $10M whole-share | UNMEASURED; R47 closes it |
| 3 | Live-account status | binds real-P&L only — **alpaca-paper is a real broker for cost measurement** (`real_money: False`, `real_broker: True`) | refinement of Grace's BIND, held as a disagreement |
| 4 | Tier-1 sleeve ceiling | $250 | design |
| 5 | Tuition budget | ~$250 | design |
| 6 | ~~Granularity~~ | does not bind at $6.25 | FALSIFIED (R47) |
| 7 | ~~PDT~~ | — | RETIRED (R45) |

## CHALLENGE (TIGHTENS — withdraws a claim the record makes)

**Target: the standing claim that the $500 beta sleeve's loop "completed measured" and that the fund has been deployed under mandate since 2026-08-19.** New evidence: the D11 venue forgery receipt plus tonight's reconcile — the broker holds zero TLT/DBC/DBA and exactly the legacy book. Measured deployment: $166.74. Remedy: (1) record falsifier #3 tripped (R43) — "completed on six of eight legs, failed on accounting" is a true and useful result; (2) fence every pre-mode-fix TCA observation as pre-instrument under the clean-field rule; (3) date leg-3 "capital deployed under mandate" from the day the broker holds it, not 2026-08-19. What would change the PM's mind: a broker fill-history reading showing the sleeve orders did reach Alpaca paper and `/venue/reconcile` is what's wrong.

## What the PM did not look at

The LEAN belt/sweeps in detail (Entry 20 numbers taken from carried BINDS); the paused legacy strategies; `sleeve_alpha_500`; the order log walk; the COO/Grace memos in full; broker cash (structurally unreadable); the market holiday calendar; `snapshot`/`nav_strike` (unobserved, unknown); Abhishek's surfaces.
