# PM R39 — the reconciliation plan (Monday 2026-08-24) + custody attribution

**Filed verbatim by the Fable chair from run `run-pm-r39`, 2026-08-23. Chair
verification before filing: `/venue/sync/plan` live with `projected_step_usd
+126.37`, `cash.delta_usd −121.85`, `unmanaged_after` = the six legacy names;
`/fund/venue/account` returns `cash: 846.84` (the PM's own E8 corrected);
`reconciliation_usd: 0.0` / `pnl_ex_reconciliation_usd: −114.26` confirmed.
**THE CUSTODY FIXTURE IS CAPTURED** (`docs/pm/CUSTODY_FIXTURE_2026-08-22.json`,
taken BEFORE any sync, per the plan's own instruction — after Monday this
evidence ceases to exist). The custody builder ticket is filed. R38 was
CEO-accepted ("Yes lets close the legacy…"); this plan is how it executes.**

---

## TL;DR

The broker and our books are two different portfolios, and the gap is **$126.37 in our favour** — the shares we thought we sold on 20 August were never actually sold, so we still own them. Closing this out on Monday takes ten small orders and costs about a dollar in trading.

The order matters and it is not the obvious one: **we must first correct the books to match the broker (one click, no market risk), then sell the six old holdings, then re-buy our four sleeve positions.** Selling first would record sales of things our books say we do not own.

Two things you should know before clicking: after step one the books will briefly show a **57.9% exposure against a 48.1% limit and a strategy-concentration breach** — both are expected, both disappear by step four, and I am predicting them now so nobody discovers them. And the routing that caused all this is fixed but **never tested with a real fill** — so order one on Monday is a $4.50 probe, and if it does not show up at the broker, everything stops.

Nine recommendations follow, each clickable on its own. Nothing here is a bundle.

## 0. Corrections first, because the counts are load-bearing

- The brief said **11** out-of-sync symbols; the endpoint says **10** (`F` is in_sync at 0/0). Ten divergences.
- Correcting the PM's own prior number: **capital deployed under mandate at the venue is $0.00, not $166.74.** The broker's SPY 0.217757 is a LEGACY lot (bought 08-14 @778.58, book-sold 08-20); the sleeve's 0.346119 lot is 100% phantom. Same ticker, different money — the reconciler nets them by symbol, hiding a two-sided $362 error behind a one-sided $98 one. **Monday is the fund's first real deployment under the current mandate.**
- Correcting the PM's own E8: **broker cash IS readable** — `/fund/venue/account` returns `cash: 846.84`, `buying_power: 6650.59`, independently confirmed to 9 cents by ledger arithmetic.

## 1. The gap, decomposed

**Book cash is $121.85 too high; book positions are $248.21 too low; net the book owes itself +$126.37.**

**ORPHANS — broker holds, ledger disclaims (6 names, $998.57):** GLD $179.71 · XLE $174.95 · SOFI $173.70 · MSFT $164.36 · NVDA $161.03 · INTC $144.82. **The phantom-GLD shadow: $137.26** — the book recorded the 08-20 sale at a fabricated $100.00 (realised "destruction" $128.26, the drawdown-rebase reason); the shares were never sold and sit at the broker UP $8.99 on cost. Five of the six carry no phantom; do not let "the phantom" label the whole reconciliation.

**PHANTOMS — ledger claims, broker holds nothing (4 legs, $750.35):** TLT 3.019871 · DBC 8.122157 · DBA 5.314306 · SPY 0.128362-of-0.346119. This class was missing from the chair's three-way taxonomy and is 43% of the divergence — custody has two directions.

## 2. Why the obvious order is wrong

R38's naive "sell legacy, buy sleeve" is unexecutable through the honest path: selling first records six SHORTS (the ledger holds zero of them; shorting is enabled and compliance only warns when it is off); buying first doubles the sleeve to ~88% gross. **The only sequence that never fabricates a fill and never restates the ledger: SYNC the book to the venue → SELL the orphans → REBUY the sleeve.**

The sync instrument exists, is built, and has never run: `POST /fund/venue/sync/apply` → one `BookReconciledToVenue` event. After it, `reconciliation_usd` reads **+126.37** and `pnl_ex_reconciliation_usd` stays **−114.26 forever** — the shadow RECORDED, never absorbed into performance. The sleeve's paper-fill inception dies with it, honestly: **the premia sleeve's real inception is 2026-08-24**, and rebuying into the same strategy_ids means every exit rule predates its entry by days — the strongest pre-commitment the fund has ever had, free.

## 3. The five phases (all times 2026-08-24)

| Phase | When (UTC) | Action | Expected after |
|---|---|---|---|
| **1 SYNC** | 12:30–13:25 (pre-open) | `GET /venue/sync/plan` → `POST /venue/sync/apply` (run_id echoed same sitting — fresh uuid per read), CEO approval channel. NOT a proposal; no 120-min clock | NAV 1,885.74 → **2,012.11**; cash SET to 846.84; positions 1,165.27 (7 names); `reconciliation_usd` +126.37; `pnl_ex` unchanged −114.26 (THE TEST); out_of_sync 0. **Predicted so nobody discovers**: gross 57.91% (temp), `discretionary` 49.63% vs 40% cap (temp), 6 uncovered $998.57. GLD's dead machinery-test rule STAYS dead (triggered rules never re-arm) |
| **2 PROBE** | 13:35 | SELL 0.05 INTC (~$4.50), one proposal, CEO clicks. The routing fix has NEVER carried a live fill | `/venue/reconcile` shows broker INTC **1.558762**. **IF NOT: STOP EVERYTHING** (fresh-account trigger 1 arms) |
| **3 CLOSE ORPHANS** | 13:45–14:15 | Six separate proposals, GLD FIRST: GLD .424471 / XLE 2.749912 / SOFI 9.18819 / MSFT .340051 / NVDA .749886 / INTC 1.558762. All under the $301.82 order cap | NAV ≈2,011.8; cash ≈1,845.4; realised **−$10.58** (the legacy book's true result, on real fills); `discretionary` empty; uncovered 0 |
| **4 REBUILD SLEEVE** | 14:30–15:00, after all six confirm | Four separate proposals at UNCHANGED quantities: DBC 8.122157 / TLT 3.019871 / DBA 5.314306 / SPY 0.128362 — each staged only after its exit rule verifies LIVE in `/exits` (`superseded:false`, no `triggered_at`). Frozen exit dates STAND (TLT/DBC 09-08 — the fund's first real exit execution, taken as a free control test) | NAV ≈2,011.5; cash ≈1,095 (54.4%, written reason: throttle caps gross); positions = the book's four, AT THE BROKER; gross **45.58% — throttle-compliant for the first time in three reviews**; exit coverage **executable 8/8 for the first time in fund history**; capital under mandate **$917.06, actually** |
| **5 ACCEPT** | 15:00–15:30 | `/venue/reconcile`: out_of_sync **0** AND residual ≤ $3.00 | If residual > $10.00 unsourceable: STOP and file (fresh-account trigger 2) |

Constraints checked once, all clear: zero day trades by construction (sell set and buy set are disjoint symbols); all ten orders under the notional cap; halt risk none (the big move is +$126 upward); nothing auto-approves — **ten CEO clicks plus one sync click**; total cost **$1–2** on $1,748.92 of turnover.

## 4. Custody attribution — the schema (the CEO's design goal)

> CEO, verbatim: *"my goal was that our harness evolves this thought process of learning to identify which portfolios and assets its managing vs what some other system/human might have logged into the account."*

**Five classes, not three**, keyed on (symbol, strategy_id, lot) — never symbol alone: **`fund_book`** (broker holds, ledger claims, agree) · **`orphan`** (broker holds; OUR harness created then disclaimed it — a routing/bookkeeping defect; today's six) · **`phantom`** (ledger claims, broker holds none — today's four legs; the class a broker-side-only taxonomy cannot see) · **`foreign`** (broker holds; NO ledger event ever created it — an actor outside the harness; zero today, highest severity when it appears) · **`unknown`** (cannot establish provenance — abstention is a feature).

**What reads it instead of assuming:** exit coverage gains an *executable* number (rule exists AND class==fund_book AND broker qty suffices — today: recorded 8/8, executable 0/8); reconcile reports per-class deltas with `foreign` excluded from fund drift; the drift alarm gets per-class severities (new `foreign` = highest; `phantom` growing = routing broken again; `orphan` shrinking = the plan working, no alarm); autopolicy's venue checks reviewed against lots not symbol totals (control layer — review recommended, nothing touched). NAV unaffected by construction.

**Builder ticket shape:** read-only projection (`app/fund/custody.py`) + `GET /fund/custody` + a `custody` field on two existing endpoints; acceptance test: **SPY produces TWO rows** (orphan 0.217757 + phantom 0.128362), never a netted −0.128362; fixture = `CUSTODY_FIXTURE_2026-08-22.json` (captured); falsifier written at birth.

## 5. Fresh paper account — what closing in place teaches, and the switch threshold

Closing in place buys four things a fresh account cannot: **the attribution muscle** (a real $998.57 orphan fixture + $750.35 phantom fixture to build and test against — the next orphan WILL come and would arrive with nothing built); **ten real Alpaca fills** (the price-stratified TCA sample, $18.91→$765.55, free); **the first end-to-end completion of propose→gate→click→real-broker-fill→clean-reconcile**; **the first exercise of `BookReconciledToVenue`.** Cost: $1–2.

**Fresh-account triggers, armed as rules:** (1) two consecutive propose→broker probe failures — then the harness is the problem, freeze and re-open clean; (2) an unsourceable residual > $10.00 at Phase 5. Neither live. **Close in place.**

## 6–7. Recommendations (nine, each one click — R39-1 gates all; 2 gates 3–4; 3 gates 4)

R39-1 sync (CEO, 08-24, hard) · R39-2 probe (CEO, 08-24) · R39-3 six orphan sells (CEO, 08-24, irreversible) · R39-4 four sleeve rebuys (CEO, 08-24) · R39-5 hold the frozen exit dates (CEO, 09-08) · R39-6 custody ticket (chair, 08-24 — FILED) · R39-7 fence the pre-08-24 round-trip cohort (chair, 08-25 — 17 recorded round trips for ~11 economic ones after Monday; fence, never restate) · R39-8 review the drawdown rebase (CEO, 08-26 — its "realised destruction $128.26" never occurred at the venue; the review is a TIGHTENING of the reference) · R39-9 review autopolicy venue checks against lots not symbol totals (CEO, 09-01 — control layer, review only).

## CHALLENGE (TIGHTENS)

To the 2026-08-21 drawdown-rebase approval: its written reason — "realised destruction $128.26" from the phantom GLD fill — is now measured false: the shares were never sold, sit at the broker up $8.99 on cost, and the reconciliation returns $137.26. The remedy is R39-8's human review (peak to the honest trailing high, a $104–128 tightening); the mechanism's own guard rail 4 already handles the direction. Filed as a review with evidence, not a change.

## What the PM did not look at

Monday's actual prices (all figures Friday's close; the acceptance test is a bound, not a point) · the belt/gate · fresh TCA (next honest read is Monday's ~13 fills) · component-vol post-sync (UNFORECAST — INTC/NVDA may alarm at Phase 1) · the divergence projection re-check · any web source.
