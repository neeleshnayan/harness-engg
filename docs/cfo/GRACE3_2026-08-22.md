# CFO dispatch 3 — Grace — the fastest safe path to the first real dollar

**Filed by the CTO chair 2026-08-22 from run `run-cfo-3`. Read-only. The
anti-fortress counterweight to the PM/riskofficer bounding pair (CEO steer:
"we should not wary off too much... deployed with real $$"). `## WHERE I
DIFFER` on the PM readiness matrix owed next round. Chair verification at end.**

## The finding

The exec table converged the calculated-risk design onto one residual (sim
rehearsal now vs after blockers). Grace is the counterweight and reframes one
level up: **both bounding seats are optimising the wrong constraint.** The
first real dollar needs neither the auto-entry-envelope nor two of the three
blockers — it needs a **live-funded brokerage account nobody has opened**, and
that is the one path item with external lead time, the only way to close
precondition P5, and **its clock is not running.**

- The whole $1,885 book is **PAPER**: `orders_are_real:false`, `env staging`,
  `ALPACA_PAPER` default true. **P5 (real informative fills) and
  "controls fired in anger" are structurally impossible on paper, forever** —
  every paper fill has `avg == arrival` to the last bit. The CEO's own
  scoreboard already requires a live account; this is a dependency inside the
  mandate, not an addition to it.
- The account is the **only** path item with external, uncontrollable lead
  time — KYC ~1–3 business days, instant-ACH near-instant, no minimum.
  Everything else is days of controllable internal work. **Scheduling rule
  when compute is free and engineering is cheap: start the external clock
  first, finish internal work just-in-time against it.** Today it is reversed.

## The decisions (for the CEO's batch)

- **G1 — open + KYC + fund a live account today.** The single largest mover;
  a strict serial dependency for 2 of 5 preconditions. **CEO's own act — no
  agent can or will open an account or move money.** Reversible (defund,
  revert the flag). Honest flag: if real money at $2k is not yet the intent,
  P5 is un-closable by definition and the $2k→$10k proof is blocked at the
  *mandate* level — a CEO decision.
- **G2 — reach the first real dollar via a CEO-clicked Tier-0 deploy, not the
  auto-entry-envelope.** The experimental authorization (2026-08-21) already
  puts the click on every deploy, so blockers 1 & 2 (which gate the *machine*
  opening risk) do not bind one clicked position with a hard pre-committed
  exit. Only blocker 3 (venue truth) is first-dollar load-bearing. Saves
  ~1–2 weeks. First real fill ~**2026-08-28**, binding on KYC.
- **G3 — wire blockers 1 (~1hr) & 3 (small) this week; object to the
  "hard 3-blocker moat" framing.** Blocker 1 is a literal copy of the
  `_guard_approval` sitting 3 lines away (`fund.py:3765/:3787` guard it,
  `:3800` does not — chair-verified). Close them fast; do not let them gate
  the click. Blocker 2 (~1–2 days) is the only one with real design content.
- **G4 — recompute this critical path every CFO dispatch** against live
  precondition states, gate discrimination D, cost-model reliability, and
  account status. Chair-standing, no decision needed.

## The over-caution named

The riskofficer frames all three blockers as "(hard)" gates before "ANY real
graduated entry." Two of them gate *automated* entry, not a clicked one — so
the framing slips the first-dollar date behind automation the first dollar
does not use. And the firm already authored the experimental-deployment
permission a month of design cycles is now stepping around. **Use the
permission you already granted; build the envelope for throughput afterward.**

## Meter (uncapped `/fund/desk/runs/stats`)

65 runs / 12,019,993 tokens; builder 42.6% (5.12M/14 runs); ~$96 lifetime,
split unmeasured; status recorded 5/65, duration 10/65 — per-seat *outcome*
ranking still too thin, so this dispatch ranks **work items**, not seats. The
constraint is not tokens and not the cheap builder work; it is the zero-token
account clock that is not running.

## Honest limit

The ~2026-08-28 date rests on a KYC lead time inferred from vendor docs, not a
filed application — external variance is real and is *exactly why the clock
should start now*. Grace has not verified the CEO intends real money at $2k or
already holds a live account; if not, G1 is a mandate-level decision.

---

# CHAIR VERIFICATION

Line-exact: `ALPACA_PAPER` default true (`alpaca.py:79`, `:12`) — the whole
book is paper, confirmed. The resume-guard adjacency — `fund.py:3765`
(halt_acknowledge) and `:3787` (loss_reference_rebase) both wrap
`_guard_approval`; `:3798-3800` resume does not — so Grace's "blocker 1 is a
~1hr fix" is correct. **THE BOUNDARY, stated by the chair as loudly as by the
seat: opening, KYC, and funding a live account is the CEO's own act. No agent
can or will do it — creating accounts and moving money are prohibited to the
machine by construction, not by policy convenience.** Grace's reframe is the
counterweight pairing working: two seats built bounds, the clock seat found
the binding constraint is an unopened account, and it converges with the
riskofficer/validator/adversary on the paper-venue fact from the date axis.
This is a proposal; it opens nothing and moves no threshold.
