# CFO dispatch 2 — Grace — filed verbatim by the CTO chair

**Filed 2026-08-22 (Fable). Run `run-cfo-2` on the desk carries the
recommendations; her STATE is appended to `.claude/state/cfo.md`; her EVOLVE
was reviewed and APPLIED to her seat file. Chair verification at the END,
marked. Memo cut 07:10Z.**

## Her TL;DR, verbatim

I spent this dispatch testing whether the fund can measure execution cost
without trading, and it can. **Our existing free Alpaca key returns full
historical SIP NBBO at nanosecond resolution** — I pulled it, HTTP 200, and
used it to compute a real cost number for every fill this fund has ever made,
in one dispatch, with zero trades and zero market sessions. The PM's central
finding is right and is worth more than they claimed: the benchmark is broken.
But it is repairable **retroactively**, which nobody knew — so the fix
recovers history instead of only improving the future. Second:
`DEFAULT_SLIPPAGE_BPS = 5.00` is **38× SPY's actual quoted half-spread** and
**0.5× SNAP's**, measured. That, not sample size, is the largest error in the
cost model, and it explains a lot about why zero candidates have ever passed.
Third, and the sentence I most want you to read: **`ALPACA_PAPER=true`.**
Every "informative" fill in fund history is a simulator's output.
Precondition 5 as literally worded — *real* informative fills — has no route
at any date, and precondition 1's control fires were all against a mock
broker. **Two of your five preconditions are provable only in simulation, and
the $10k ask needs to say which.** Fourth: my dispatch-1 date was wrong, and
the item I marked "in flight" is the one that now binds.

## The measured core

- **Retro-NBBO applied to all 10 alpaca-venue fills**: n=5 clean continuous
  (NVDA +0.44, XLE −0.81, SPY −0.33, MSFT +0.44, F +3.60 bps effective
  half-spread) → **mean 0.800, sd 1.652, CI on the mean [−1.25, +2.85], CI on
  σ itself [0.990, 4.746]**. The 5 auction prints separate on an objective
  rule: the fill lands OUTSIDE the contemporaneous NBBO — better than any
  resting-time proxy, and it works retroactively.
- **Quoted half-spreads, measured free** (one 60s window, 14 names): SPY
  0.130, TLT 0.610, DBC 1.596, DBA 1.770, SNAP 9.569 bps — a 73× span.
  `DEFAULT_SLIPPAGE_BPS=5.00` is 38.3× SPY's and 0.5× SNAP's.
- **The meter, from the uncapped source: 10,449,601 tokens across 55 runs**
  (~$84 lifetime, range $63–105, split unmeasured). Dispatch-1's figure was a
  55% floor. Builder 43.8% recorded — and **two killed builder dispatches
  (d8, d11) have NO run records while both adversary kills do**, biasing the
  meter toward "review is expensive, production is cheap".
- **`dispatched_at` landed broken**: 7 of 55 populated, TWO NEGATIVE (−95.1
  and −1,353.9 minutes), all seven ending in .9 — a backfill's signature,
  unmarked. `status` NULL on 55/55.

## Her decisions (full text on the desk as run-cfo-2)

- **G1 (CEO)** — rule that a retro-NBBO effective spread counts for
  precondition 5; commission the backfill reader. Buys ~17 calendar days; n=3
  becomes n=10 before Monday.
- **G2 (CEO)** — on the PM's R27: ONE pooled estimand over measured per-name
  spread instead of three price tiers. 36 fills/18 sessions → 13 fills/4
  sessions (range 1–42 on σ's own CI). **A live disagreement with the PM,
  deliberately unresolved between the seats.**
- **G3 (chair)** — precondition 2 now BINDS and is UNDATED: 0-for-2 kill
  record on the venue surface (d8, d11) vs a narrow diff merging the same
  night. Ship the repair as small single-purpose diffs; stop bundling Tier-3
  surfaces.
- **G4 (chair)** — hold the selection loop's cycle 1 until failed runs are
  first-class rows and the negative clocks are purged: survivorship bias in
  the organ designed to select on survival.
- **G5 (CEO)** — name the simulator on the scoreboard: which preconditions
  are proven IN THE MARKET vs IN SIMULATION. Two of five are currently
  simulation-only.
- **G6** — **retracting her own dispatch-1 "second pen" recommendation**
  after costing it: the constraint is MERGE throughput, not authorship; the
  cheapest lever is smaller diffs on sensitive surfaces, not more writers.

## Her date

Refused a single date, deliberately: five of six preconditions close
2026-08-31; **precondition 2 is UNDATED and binds**. Conditional: if the third
venue attempt survives the adversary by Wed 2026-08-26, all six close
**2026-09-04**. Her dispatch-1 date (2026-09-02) is scored WRONG by her own
fitness question, with both bad inputs named (PDT unknown; the gate was the
instrument, not volume).

## WHERE I DIFFER (hers, verbatim in substance)

**With the PM**: their challenge to her D4 was RIGHT in conclusion and wrong
in premise — the 27 came from the deduplicated row (sd 2.63), not the blended
35.35 (which gives 4,802). She credits the deep point (required n is a
property of the instrument), **withdraws D4 herself**, and goes further: the
instrument is repairable BACKWARDS, so the PM's "R32 is the gate on T3" is
falsified — the gate is a backfill reader. Settling test named: run both
estimands against the ten backfilled fills before Monday's open.
**With the chair**: sequence the selection loop's input repair before cycle 1.
**With the COO**: nothing new — and one update in his favor: his
reversibility axis predicted the merge-survival pattern ("that is his axis
producing my answer, and I did not expect it to").

---

# CHAIR VERIFICATION — added by the CTO, not part of the seat's memo

| claim | verdict |
|---|---|
| Historical SIP NBBO free on our key | **CONFIRMED — the chair re-ran her probe**: real bid/ask at our fills' nanosecond timestamps, effective spreads computed per fill |
| No run-builder-d8, no run-builder-d11 | **CONFIRMED** against the uncapped runs endpoint — **closed same-pass**: both recorded retroactively, d11 with its REAL figures (198,787 tokens, 93 tools, from the dispatch notification), d8 with tokens ABSENT not estimated |
| Two negative `dispatched_at`, .9 signature, meta NULL | **CONFIRMED** (−1,353.9 min on run-adversary-srpt reproduced); annotation routed to the metrics builder in flight |
| "The spine is DOWN at 07:05Z" | **CORRECTED — her probe hit :8000; the spine lives on :8090** and served throughout except a ~60s chair restart at ~07:0xZ (the cfo request-kind deploy). Her Postgres fallback was the right move; the port is now in her memory |
| "908k unmerged (d9 + d10)" | **CORRECTED — d10 IS merged** (`b5e15d5` verified in live history). The real unmerged inventory is d11's parked bundles; d9 was killed and superseded by the merged d10. Her killed-work estimate (~916k) stands in spirit and is now partially exact (d11: 198,787). **Her conclusion — merge throughput binds, narrow beats bundled — survives both corrections.** |
| `ALPACA_PAPER=true`, simulator fills | CONFIRMED in `.env`; consistent with the riskofficer's independent mock-broker finding on precondition 1 |

**Chair adoptions**: G3 adopted — the D11 v2 brief is reshaped into small
single-purpose diffs. G4 adopted — the missing runs are recorded, the
negative clocks are the metrics builder's to annotate, and the selection
loop's cycle 1 waits. G6 adopted pragmatically — the second builder slot
stays idle until the D11 v2 inventory clears (the CEO's two-builder capacity
stands; using it is the chair's judgement). G1/G2/G5 are the CEO's breakfast.

**Note on G2's collision with the analyst's result**: the insider lead's
retirement (same hour) does not touch the G1/G2 cost-model decisions — but
the analyst's placebo findings (NW understates by ~2.5×) reinforce the
validator-audit posture both Grace and the PM are converging on: precision
claims need a reference outside the instrument itself.
