# Grace - Token Ledger #2 + the learning-value axis debut (run-cfo-6), 2026-08-23
**Filed verbatim by the chair at resolve. Chair notes: (1) D2 (sever D19) DECLINED AS SUPERSEDED - the full D19+D20 bundle was already under blind review when this memo was cut; her UNBUNDLING DOCTRINE is adopted going forward. (2) D4's release condition for shelf v2 was already satisfied - Ed filed per-lead consumed/rejected in batch #2 before her cut. (3) D5 accepted: THE CLEANUP's ninth deferral stands ON MEASUREMENT; the chair strikes its own "non-negotiable" escalation.**

```
TO           CEO · CTO chair
FROM         Grace · CFO
DATE         2026-08-23 (UTC), cut at 07:50Z
SUBJECT      Token Ledger #2 + first run of the learning-value axis — Sunday cost 1.79M tokens and ZERO containers, and the scoreboard did not move one square
DECISIONS    5 recommended · 0 requiring the CEO's click today · 1 requiring one chair act on Monday
```

## TL;DR

Sunday spent **1,790,795 tokens across 7 runs and zero belt containers** and bought one merge, seven confirmed defects, three truthful candidate kills and an upgraded gate spec. It moved `prod_gate` by **nothing**: `n_met 1, n_blocking 4, reachable false, fills 8/20` — byte-identical to my read 8 hours earlier.

**It is because the scoreboard cannot move.** `mode.py:473` computes `reachable = bool(PROD_UNLOCKED) and not unmet`, and three of the five preconditions (P2, P3, P4) carry an `unevaluable_reason` and **no evaluator at all** (`mode.py:408-450`). They return `"unchecked"`, which is `!= "met"`, forever. **The fund can complete 100% of the work the CEO named and its own gate will still read `reachable: false`.**

**Precondition 3 is DONE.** `riskmonitor.py:411` now reads `unrealised_pnl_pct(qty, mark, avg_cost)` — signed by position direction — with six pinning assertions at `tests/test_hazard_batch.py:756-794`. The precondition's own stated verification method is *"verified by the test that fails if the inversion returns"*. **That test exists and passes, and the gate says `unchecked`.** P4's substance is close behind: `resume_trading` was the last unguarded risk control and was closed 2026-08-23 (`app/api/v1/fund.py:4257-4271`).

So the binding item is not any of the work. **It is one adversary dispatch that nobody has requested** (the evaluability half of ticket `a0e640de` — a loosening, correctly waiting on an adversary blind that has not been filed). Zero net-new tokens; it unblocks the only build that can turn the scoreboard true.

Second finding, arithmetic: **Monday's R39 sheet lands P5 one to three fills SHORT.** Eleven orders planned; 8 + 11 = 19 against 20 required, at an observed 80% yield of measurable execution legs → ~17. **Three more $4.50-scale probes — about $15 — close P5 on Monday instead of a second session.**

**I hold Fri 2026-08-28** for the $10k ask, with a single-act falsifier below.

## THE DECISION LEDGER

| # | Decision | Cost | Moves the date? | Recommendation |
|---|---|---|---|---|
| 1 | Request the adversary blind on `a0e640de`'s P2/P3/P4 evaluability half, Monday | ~200k (runs anyway) | **YES — it IS the date** | **DO IT FIRST** |
| 2 | Sever and merge D19's cleared half now | 0 net (severability MEASURED) | YES — unblocks the 08-25 deadline | SEVER *(chair: superseded)* |
| 3 | Amend Monday's fill sheet to ≥12 measurable-execution orders | ~$15 notional | **YES — closes P5 one session early** | **AMEND** |
| 4 | Ed batch #3 next mechanism slot; Doc's shelf v2 deferred | ~186–314k | Indirectly (funnel at ZERO) | DISPATCH ED |
| 5 | THE CLEANUP — NINTH deferral, on measurement | ~300k avoided | No | **DEFER AGAIN** |

## THE METER

Sunday 00:00–07:50Z, source `/fund/desk/runs/stats` (complete): 7 runs — validator 193,291 / secretary 196,814 / builder-d19 465,995 / adversary-d19 226,739 / ed-batch2 185,588 / adversary-edbatch2 161,940 / builder-kg-v1 360,428 = **1,790,795 tokens, 538 tools, 1 merge, 0 containers, 0 fills, 0 surviving candidates.**

**Lifetime: 93 runs / 18,222,483 tokens.** Builder share 41.8% (down from 43.1%, from 55% at founding). Findings/M (my segmentation of prose; a ranking, not a measurement): **mechanism 43.1 — highest ever recorded; adversary 18.0; builder 8.5** (was 1.2 at founding).

**Priced honestly:** (a) the double-kill = 347,528 tokens, prevented ~330k of quant + two belt cohorts + two units of family-wise budget — **net token-positive ~2.0×** before the unmeasured deployment error avoided. (b) D19 kill-and-rebuild vs merging: the rebuild ships FP 2.90%/power 40.7% vs the killed 5.00%/32.5% — at 5.00%, the first honest pass this fund ever sees carries a 1-in-20 chance of being a null. **The kill upgraded the spec.** (c) Ed cumulative: 849,115 tokens → 3 candidates → 3 kills → 0 belt. **283,038 tokens per truthful kill; leg 2 is ZERO this week.**

## THE ALLOCATION CALL

**Binding constraint: NOT tokens — the EVALUABILITY of the fund's own success criteria.**

**D1 — the adversary blind on evaluability, first act Monday.** P2/P3/P4 can never read `met`. Two of the three "unevaluable" reasons are wrong on their own terms (they describe checks a build can run); only P2 genuinely needs a human — expressible as a signed attestation event (my G5-2 loosening, kill condition attached, verdict still owed). **Four of five preconditions will be substantively satisfied within days and the instrument will report `reachable: false`.**

**D2 — sever D19.** *(Chair: declined as superseded — the full bundle was under blind at cut.)* The doctrine stands: **a bundle merges at the speed of its worst item, and blind review here is nearly free — the optimal commit size in an agentic firm is SMALLER than in a human firm.** Corroborated by Vishesh's independent nought-for-two on bundled broker diffs.

**D3 — amend Monday's sheet.** `mode.py:402` counts `informative and execution_bps is not None`; live 8/20; R39's 11 orders → 19 at perfect yield, ~17 at the observed 80% (n=10). Three more $4.50 probes close P5 a session early. **The best dollar-return item this seat has filed.**

**D4 — Ed batch #3 next: highest learning-value per token on the floor.** Two beliefs sharpened at zero container/family-wise cost: does the funnel produce a survivor, and **does moving the reviewer's controls into the generator change the survival rate** (card items 12–14; batch #3 measures it as a free by-product). On the CEO's "one strategy that genuinely sticks": **the only live path this week is Entry 20's re-judge, gated on D20. The funnel behind it is empty.**

**D5 — THE CLEANUP: ninth deferral, on measurement.** I hunted the token case FOR it and refuted it: builder tokens/dispatch 384,730 → 334,044, tools 211 → 149 across 12 runs despite ~+20k lines. The nine wrong-worktree landings (`builder.md:13,23,38,55,82,111,298,436`) are a DISPATCH-TIME defect the CLEANUP would not touch — the fix is one line in the chair's dispatch procedure. Reopen on a measured cost, not a deferral count.

## THE TOKEN LEDGER

1. **Meter since last run**: 11 runs / 2,472,483 tokens → 1 merge, ~14 findings, 3 kills, 0 fills. Builder findings/M up 7× from founding.
2. **Largest measured waste: 349,619 adversary tokens killing three candidates on falsifiers the candidates themselves declared and ran incorrectly.** Recoverable fraction under 100% (failed attacks carry value) — the recoverable part is the kill cycle on candidates that should have self-killed in Ed's own run. Runners-up: the D19 bundle blockage (~6–8h); the broken clock (3 of 22 `dispatched_at` stamps impossible — my unit is TIME and chair latency is still unmeasurable).
3. **The one economics improvement: copy every reviewer control into the generator's checklist and measure the survival rate.** Cost to find out ZERO — batch #3 produces the number.
4. **Tokens are not what binds this week** — evaluability is. One line, moving on.

## WHAT WE ARE NOT USING

1. **`kg_edge` is EMPTY** — provenance is what makes a graph a graph; "did Doc's shelf produce a candidate?" is unanswerable by query (I answered it tonight by reading memos). v1.1 is ticketed.
2. **`fund_agent_runs` has no hypothesis key — 92 of 92 verdicts unlinkable.** My founding question ("what did the tokens buy") is structurally unanswerable at candidate level until one column exists.
3. **The five-agent cap ran sequential for six hours** — ~3.4h recoverable, **labelled UNRELIABLE** (measured with the same broken clock).
Deflated in the same breath: 19,190 delisted assets, 49,409 unopened Form 4s, the idle 4090 — all real, none move either date.

**GENERALISES**: (1) a prose success criterion unimplemented in code reads not-met forever — every condition must be machine-evaluable or explicitly human-attested, no third state; (2) reviewer controls are free to copy into the generator (~160k review kill → 0-token self-kill); (3) **optimal commit size in an agentic firm is smaller than a human firm's** — we inherited the human bundling default. **RETRACTED from my architecture**: benchmark-population correction downgraded to labelling (D19: the correction has no executable path).

## THE DATE

**$10k ask / `prod_gate.reachable`: Fri 2026-08-28 — HELD.** First real VENUE fill: Mon 08-24 — HELD. P5: NOT Monday as written (see D3).
**NEW FALSIFIER, single-act and dated: if the adversary blind on the evaluability half is not DISPATCHED by Mon 2026-08-24 EOD, the date moves to Fri 2026-09-04.** Second: if Monday's sheet is not amended to ≥12 measurable-execution orders, no date before Tue 09-01 is honest. `PROD_UNLOCKED` remains a human act I will not cost.

## WHERE I DIFFER (pre-read written before opening TRIAGE6; held on all six)

1. **Vishesh under-sold his own top item**: his "decision 2 moves no date" is wrong in his favor — `mode.py:434` makes the kill switch precondition 4, so it IS on my axis. The twist: **his top item is substantively closeable this week and instrumentally invisible.** He ranks what cannot be taken back; I rank what the scoreboard can read. Settled by whether `n_met ≥ 4` by 08-28.
2. **The coverage gap**: 56 of 89 desk requests (62.9%) target the builder and **neither exec seat reviews that block by default.** The CEO should know rather than assume.
3. **Verification latency**: his measurement verified and worsened (33.0 → 38.5 → 46.36h). A fourth reading has learning value ZERO — the shelf holds three monotone readings; **what is missing is a decision, not a measurement.**
4. **Where he persuaded me (recorded, not laundered)**: his rule — *a change with no mechanical oracle does not merge same-day* — tested twice Sunday, passed twice (D21 merged on an oracle; D19 killed without one). **Adopted, with my extension: because review is cheap, UNBUNDLE.**
5. His counter-predicate challenge: with the adversary, correctly; the number has since gone 91 → 118 with 111 decided-awaiting-execution — his filing-end diagnosis looks better, not worse.
6. His G1 concession accepted.

## THE HONEST LIMIT

Findings/M is my segmentation of prose (`kind` is still free text). The concurrency figure rests on a broken clock. The power figures are synthetic (n=6000, not our candidate distribution). The builder-cost trend is uncontrolled for brief scope (n=12). I did not verify P4's call-graph wiring end to end (riskofficer's lane). **I own an error on the record**: `fund.py:4269` — I wrote "six sibling endpoints" from another seat's report; the true number was eight. See EVOLVE.

## STATE

**MY DATES, SCORE THEM.** $10k / reachable: **Fri 2026-08-28 HELD**; falsifier = evaluability blind not DISPATCHED by Mon EOD → 09-04. Sheet not amended to ≥12 → no date before Tue 09-01. First venue fill Mon 08-24 HELD.
**THE FINDING**: mode.py:473 + :408-450 — P2/P3/P4 unevaluable forever; **P3 substantively DONE** (riskmonitor.py:411; tests/test_hazard_batch.py:756-794) and the gate cannot say so; P4's last unguarded control closed (fund.py:4257-4271). Binding item = ONE UNDISPATCHED ADVERSARY REVIEW.
**P5 ARITHMETIC**: live 8/20; R39 = 11 orders → 19 at 100% yield, ~17 at observed 80%; three more $4.50 probes close it.
**METER**: 93 runs / 18,222,483 lifetime; Sunday 1,790,795 / 0 containers / 0 fills / 1 merge; findings/M mechanism 43.1 (record), builder 8.5.
**FUNNEL**: 849,115 tokens → 3 candidates → 3 kills → 0 belt; leg 2 ZERO; largest waste 349,619 (self-killable kills); card 12–14 is the fix, batch #3 measures it free.
**REFUTED MY OWN HYPOTHESIS**: no token case for the CLEANUP (builder cost flat-to-down; wrong-base landings are dispatch-time). NINTH deferral on measurement.
**ADOPTED FROM VISHESH**: no-oracle-no-same-day-merge (tested twice, passed twice) + my extension: UNBUNDLE.
**ARCHITECTURE**: three GENERALISES (evaluability; reviewer-control copying; smaller-commits); RETRACTED benchmark correction → labelling.
**OWED NEXT**: the blind dispatched Monday (my falsifier); n_met ≥ 4 by 08-28; batch #3 kill rate vs 3/3; the G5-2 attestation verdict; Doc's v1 consumed/rejected before v2. *(Chair: the last was already satisfied at cut.)*

## BINDS

**cto**: file the evaluability blind Monday before any builder dispatch; sever D19 *(chair: superseded)*; set worktree bases explicitly (nine wrong bases is a dispatch-procedure defect).
**riskofficer**: name what a machine could read to prove the kill switch is wired (call-graph assertion + test name), or say P4 belongs in the human-attestation class.
**mechanism**: batch #3 reports its own kill rate against the 0-of-3 denominator explicitly.
**analyst**: shelf v2 held until v1's consumed/rejected *(chair: already filed in batch #2)*; the rule stands — never generate supply against undrawn inventory.
**builder**: kg_edge is empty and fund_agent_runs has no hypothesis key — one column each; ask for the slot.

## EVOLVE (accepted)

**AND A COUNT IS A FIGURE.** When you write "N endpoints/rows/controls", **run the count yourself against the source**, even when a trusted seat already published it. Measured: this seat wrote "six sibling endpoints" from the riskofficer's report; the true number was eight (`fund.py:4269` carries the correction permanently). A borrowed count inherits the other seat's scope decision, which you cannot see and did not make. This seat's authority is that its numbers are re-derived.

**Primary record: run `run-cfo-6` in the flight recorder; pre-read at session scratchpad `grace6_preread.md` (independence provable).**
