# Grace 4 — 2026-08-23

**Filed verbatim by the Fable chair from run `run-cfo-4`. Concurrent with COO
triage #6; neither read the other; Grace's own ranking and her prior on the
sim-vs-bound residual were written to scratchpad BEFORE the charter, the PM
matrix, or the riskofficer envelope were opened (independence artifacts:
`grace4_own_ranking_BEFORE_reading.md`, `grace4_prior_on_residual.md`).
STATE appended to seat memory; BINDS carried; EVOLVE applied at chair review.
CHAIR NOTE AT FILING: G7 (the d11v2-import merge) was EXECUTED while this
dispatch was in flight — the CEO ran the merge himself, the chair resolved the
one conflict, 1732 tests green, spine restarted; P2 and BLOCKER 3 (venue
truth) are closed as of this filing. Read G7 as done and the blocker count in
G8 as two, not three.**

---

```
TO           CEO
FROM         Grace · CFO
DATE         2026-08-23 (UTC — the day rolled over mid-dispatch; Monday is 2026-08-24)
SUBJECT      The honest re-judge has a hole in it, and 60% of this week's tokens are parked behind one command
DECISIONS    4 requiring the CEO · 1 requiring the chair only
AT RISK      $0 deployed · DATED: G1 Monday 2026-08-24; the 2026-09-08 short hazard, 16 days
```

## §1 — TL;DR

The gate-v5 trio you approved fixes the breakeven check. It does not fix the benchmark's **population** — and this firm measured that population's bias at **−6.90pp ± 2.40 over 20 months** on 2026-08-17 and never wired the correction in. The charter's proof of honesty is "Entry 20 re-judged under v5 — whatever the verdict, it will be true." It will not be, by roughly 10pp on a 2.4-year window, **in the direction that kills candidates**. Half a builder day fixes it and the machinery already exists.

Second: **655,678 tokens — 59.7% of everything spent since my last run — are sitting in `d11v2-import` behind one merge command.** They close P2, the precondition I called undated and binding.

Third: the three controls the riskofficer says must be wired before any real entry are CEO-accepted and **have no ticket**. I grepped all 66 requests; zero hits. The PM's 2026-08-26 date for them is true only if they jump 23 already-approved builder items.

I went hunting for the fourth invented constraint and found one — our compliance module enforces the PDT rule, which FINRA retired on 2026-06-04. **It is not on the critical path and I say so below rather than lead with it.**

**Dates: first real dollar Thu 2026-08-27. Honest $10k ask 2026-09-11, and the binding item is P1, not P5.**

## §2 — The decision ledger

| # | Decision | Cost of being wrong | Reversible? | Recommendation |
|---|---|---|---|---|
| **D1** | Correct the benchmark **population** before the Entry 20 v5 re-judge | A contaminated "honest" baseline under the clean-field rule; ~10pp mis-stated excess, kill-direction | Reversible (a script + one 25-min belt run) — **but not after the verdict is banked** | **SEQUENCE IT FIRST.** Chair action; no CEO click |
| **D2** | Merge `d11v2-import` — one command | P2 stays open; 655,678 tokens stay unrealised; the spine keeps contradicting itself | Reversible (one revert) | **RUN IT.** `git -C ClarkHarness merge --no-ff d11v2-import` |
| **D3** | Ticket the three controls as ONE batch and rank it ahead of the 23 | The account funds with an unguarded off-switch and no integrity producer | Reversible | **AUTHORIZE the jump.** Endorses the PM's sprint; adds the queue position that makes his date real |
| **D4** | The drill set: manual halt+resume now, and a deliberately-rejected real order once funded | P1's disqualifying exception stands undated; P4 stays "registered", not "tested" | Reversible; $0 at risk | **DO BOTH.** 20 min each |
| **D5** | Retire the PDT block (`compliance.py:59/64`) | We keep a control with no legal basis and a wrong premise inside the PM's sizing design | Reversible | **LOOSENS — adversary blind first.** Explicitly OFF the critical path |

---

## §3 — THE METER

Since `run-cfo-3` (resolved 2026-08-22T07:35:49Z). All figures `GET /api/v1/fund/desk/runs/stats` and per-run details — note the prefix is **`/api/v1`**, not the bare path in the API card.

| run | tokens | wall clock | realised? |
|---|---|---|---|
| `run-builder-d14` (D11 v2) | 495,861 | 2h39m | **NO — parked** |
| `run-adversary-d11-v2` | 159,817 | 35m | **NO — parked with it** |
| `run-quant-entry20` | 175,235 | 2h04m | yes |
| `run-builder-d15` (bar cache) | 267,691 | 41m | yes |
| **total** | **1,098,604** | | **59.7% unrealised** |

**Lifetime: 71 runs / 13,290,774 tokens**, `truncated: false`, `complete: true`. Builder **44.3%** (5,884,363), up from 42.6% at my last run. At my Grace-2 unit (~$8.04/M) the delta is **≈ $8.83**; the parked 655,678 is **≈ $5.27**. *The dollars are not the cost. The cost is that P2 stays open while they sit there.*

**What I still cannot see, reported absent rather than modelled:** 55 of 71 runs carry no `dispatched_at`, so wall-clock is UNKNOWN for all but 16; 60 of 71 state no outcome, so the endpoint's "0 failures" is a **floor**, and it says so itself; 4 runs carry no token count and contribute nothing rather than zero. **And an unexplained +2:** my Grace-3 STATE recorded 65 runs; 65 + 4 = 69, not 71. Two rows I cannot attribute. Flagged, not smoothed.

**What the spend bought, in time:**
- **d15 — the 1,300× is not the return.** Per-leg 2,125ms → 1.64ms, 170/170 byte-identical, candidate 96.4 min → ~20–25 min projected. But the belt ran **96.4 min against a 10,080-min week at ~1 candidate/week — a 0.96% duty cycle.** A 4× on a stage at 1% duty cycle moves nothing today. **The actual return on d15 was the defect it surfaced while instrumenting**: `marketdata.py:381` routes any start+end call to Yahoo while strategies trade Alpaca, so *every benchmark the belt ever computed covered one session less than the curve it graded.* That is worth more than the speedup and it was incidental to it.
- **quant-entry20** — the first substantive pass, plus three belt defects, all pass-favourable.

---

## §4 — THE LEVER MAP

Extending the inherited map; I do not re-derive what is already in it.

| lever | current | set by | measured effect | cost to move |
|---|---|---|---|---|
| **Builder queue ORDER** | unranked, FIFO-ish | nobody — emergent | 23 approved + 9 open = 32; oldest **32.92h**; `decided_awaiting_execution: **110**` | **Zero.** One chair decision |
| **Builder concurrency** | 1 heavy job | CEO 2026-08-22 (2 allowed, disjoint scopes) | **APPROVED AND UNPULLED**. Upper bound 2×; realistic 1.4–1.6× if suites are 30–50% of wall clock — **UNMEASURED** | Free to measure: one dispatch timestamping edit-vs-suite |
| **Builder median duration** | 3,826.7s (63.8 min) | — | n=**3 of 16** — 81% of builder runs lack `dispatched_at`, so this median rests on 19% of the population | Closing from d13 forward, never retroactively |
| **Benchmark population** | live-`ACTIVE` only (`universe.py:115`) | inherited | **−6.90pp ± 2.40 / 20mo**, measured `SURVIVORSHIP_2026-08-17.md`, n=26 — **and never wired in** | ~half a builder day; `asof.py` exists |
| **PDT block** | `PDT_MAX_DAY_TRADES = 4`, live `applies: true` | inherited | **Enforces a rule retired 2026-06-04.** Broker returns `pattern_day_trader: null` | ~1h + adversary pass (LOOSENS) |
| **Vendor entitlements** | audited 3× now | this seat | **3rd instance tonight**: 19,190 delisted US equities + their historical SIP bars, free, on the key we hold | $0 |
| **Belt cost** | ~20–25 min/candidate projected | builder d15 | 1,300×/leg, 170/170 byte-identical | merged |
| **Cost model** | sample 8, `reliable: false` | inherited | NBBO makes required-n **small** (n=5 gave a usable CI in my dispatch 2) | $0 — entitlement held |

---

## §5 — THE DECISIONS IN FULL

### D1 — Correct the benchmark POPULATION before the Entry 20 v5 re-judge

**WHAT** — Before the re-judge runs, make the belt's benchmark read as-of membership instead of the live active universe.

**WHY NOW** — The charter's honesty proof is *"Entry 20 re-judged under v5 — whatever the verdict, it will be true."* Three things make that false, and only two are being fixed:

1. `universe.py:115` builds the hunting ground with `status=AssetStatus.ACTIVE` — names alive *today*.
2. This firm **already measured** what that costs: **−6.90pp ± 2.40 over 20 months** (`SURVIVORSHIP_2026-08-17.md`, n=26, PIT membership from `fund_universe_asof`, band-eligibility decided on prior information).
3. The correction has **no consumer.** `asof.py` — a module written for exactly this, with `membership(as_of)` at `:215` — is imported by **one file in the repo**, `scripts/capture_asof.py`. Not the belt. Not the gate. Not the universe builder.

**And I had the direction wrong until I read the doc, which is why I am citing it rather than reasoning from first principles.** Delisting is not failure here: the vanished band names *gained* +12.1% mean, mostly acquisitions. The bias is still flattering, but because the vanished gained *less* than the survivors (+12.1% vs +60.9%). So the survivor-only benchmark is **too high**, which makes it **too hard to beat** — the error runs in the **kill** direction, in a firm whose scoreboard line is *zero candidates have ever passed the gate honestly* and whose north star is *not get happy about killing ideas.*

**HOW / the arithmetic** — Entry 20: benchmark 84.78% over 2.4 years. Scaling −6.90pp/20mo gives ≈ **−9.8pp** over that window → corrected benchmark ≈ **75.0%**. The quant's like-for-like excess of **+21.98pp** becomes roughly **+31.8pp**, before whatever smaller haircut applies to the strategy leg. **This is an ESTIMATE, not a measurement** — different window, different band snapshot, n=26. I offer the order of magnitude, not a correction to apply.

**RECOMMENDATION** — Chair action, no CEO click. Run the as-of capture for the backtest start, point the benchmark construction at `membership(as_of)`, re-run. Cost: ~half a builder day plus one ~25-min belt slot — **smaller than the gate-v5 trio already approved and dispatched.** If it slips behind the re-judge, the clean-field rule bites: we bank a verdict labelled *honest*, and every later candidate is measured against it.

---

### D2 — Merge `d11v2-import`

**WHAT** — `git -C ClarkHarness merge --no-ff d11v2-import`.

**WHY NOW** — 655,678 tokens, **59.7% of the delta since my last run**, are in that branch. It is adversary-cleared (SURVIVES/SURVIVES, all eight kills closed), you approved the crossing, and the chair correctly stopped rather than work around the permission layer. It closes **P2** — `GET /fund/book` currently returns `venue: "alpaca"` with `orders_are_real: false`, the spine contradicting itself — and **BLOCKER 3** of the riskofficer's three.

**Scoring my own prediction:** Grace 2 said P2 was undated and binding on a 0-for-2 repair record, with a conditional 2026-09-04 *if the third venue attempt survives by Wed 2026-08-26.* **It survived on 2026-08-22, four days early.** That prediction verified in the favourable direction; the date it was gating is now available and the only thing between us and it is one command.

**RECOMMENDATION** — Run it first in wall-clock order, ahead of everything else in this memo, because it is one command and it is certain.

---

### D3 — Ticket the three controls as one batch, and rank it ahead of the 23

**WHAT** — File the resume guard, the integrity-halt producer, and venue-from-connector-identity as one builder batch, and dispatch it **ahead of** the 23 already-approved items.

**WHY NOW** — Verified tonight, line-exact:
- `fund.py:3824-3827` — `resume_trading` calls `_control.resume(actor=req.actor)` with **no `_guard_approval`**, on a CORS-only API. Six sibling endpoints in the same file use the guard: `:1463, :2391, :3684, :3758, :3792, :3814`. `halt_acknowledge`, which acts on nothing, **is** guarded.
- I grepped **all 66 desk requests** for `resume` / `integrity halt` / `off-switch`: **zero hits.** These are CEO-accepted via COO Batch 3 and Tier-3-deferred, and they exist only as prose in `DAY_LOG.md` under ON FIRE. **An alarm that does not create a work item is decoration.**

**This is Vishesh's rule, and I am applying it because he wrote it.** Triage #5: *"a seat asserting a measurement is queued is not the same as a queued measurement. Verify the ticket, never the sentence."* He returned R27-vs-G2 on it. I checked because of him, and it paid twice — the three controls, and `asof.py`'s single consumer in D1. That is a legitimate cross-seat update and I am making it visible rather than laundering it.

**HOW / the arithmetic** — The build is small: the resume guard is a ~1h copy of an in-file pattern used six times; the integrity alarms are already *built* at `riskmonitor.py:967-989` and simply not read by `run()`, so it is wiring, not authoring. Call the batch **one builder day.** The queue ahead of it is 23 approved items at a measured median 63.8 min — **~24 builder-hours, or 4–6 calendar days at one heavy job.** The account funds Tue–Thu. **Filing and ranking costs minutes and buys 4–6 days.**

**RECOMMENDATION** — Authorize the jump. This endorses the PM's three-control sprint and supplies the one thing his 2026-08-26 date is missing: **he named a date without naming the queue position that makes it true.**

---

### D4 — The drill set: two engineered events, $0 at risk

**WHAT** — (a) Pull the kill switch by hand, once, on the record, and resume. (b) Once the account funds, submit one order deliberately designed to be **rejected** by the real broker — more shares than settled cash — and observe our controls handle it.

**WHY NOW** — The riskofficer measured today that **P1 is met with four exceptions**, and the disqualifying one is: *"EVERY FILL IN THIS FUND'S HISTORY WAS MOCK-FILLED — last fill seq 594 has `avg_price == arrival_price` to the last binary digit — so no control has ever met a real broker's partial, reject or account state."* Also: no human has ever pulled the switch; all eight halts carry payload keys `['reason']` only; the class machinery has never processed a classified halt.

**The part nobody has proposed:** a real broker rejection is the *cheapest possible* real-broker adverse event. It costs nothing, risks nothing, fills nothing — and it is the only same-day way to put one of our controls in front of genuine venue behaviour. We have been waiting for the market to hand us an adverse event. **We can order one.**

**RECOMMENDATION** — Drill (a) this week, before funding. Drill (b) the day the account funds, before any deliberate deployment. Twenty minutes each. Together they close the observable half of **P4** and start on **P1's** exception. Note the sequencing: run (a) again *after* the resume guard ships, so the second drill tests the guard rather than its absence.

---

### D5 — Retire the PDT block — LOOSENS, and it is OFF the critical path

**FIRST LINE, per the protocol: this would REMOVE a control. It goes to the adversary blind before it reaches you.**

**WHAT** — Retire `PDT_EQUITY_THRESHOLD = 25_000.0` (`compliance.py:59`), `PDT_MAX_DAY_TRADES = 4` (`:64`) and the block at `_pdt_blocks` (`:233-278`).

**WHY** — The rule no longer exists. SEC approved the FINRA Rule 4210 amendments 2026-04-14; FINRA published Regulatory Notice 26-10 on 2026-04-20; **effective 2026-06-04**, eliminating the pattern-day-trader definition, the day-trade count thresholds, and the $25,000 minimum. Alpaca implemented it and lowered the equity floor for 4× intraday buying power from $25,000 to $2,000. Our own broker now returns `pattern_day_trader: null, daytrade_count: null` — **the only thing enforcing PDT on this fund is this fund.** Live endpoint still says `"applies": true, "remaining": 3`. Eleven weeks stale.

**AND IT IS ALREADY CORRUPTING A LIVE DESIGN.** `PM_GRADUATED_SIZING_2026-08-22.md:86`: *"The binding leg is PDT + granularity, NOT confidence"*, and `:85`: *"40 positions turning every 3 days annihilates PDT."* That is wrong on **two** independent grounds. The rule is retired — and even under the old rule, our own `would_create_day_trade` (`:191-199`) correctly counts only *same-session opposite-side fills in the same symbol*, and `count()` (`:180-183`) only *"round trips completed inside a single session."* **Entry 20 holds ip+3. It generates zero day trades by construction.** The residual concern is same-day re-entry collisions — a scheduling rule, not a size cap.

**WHY I AM RANKING IT FIFTH, AND DECLINING MY OWN HEADLINE.** I found this hunting for a rate limiter on P5 and I want to be honest that it is not one. The validator's BIND to me says the spread term of cost is **free and already measured** via NBBO, and π is expensive and second-order — so a high-volume fill programme buys the non-binding term, which is this seat's own named failure mode. And a rejection/partial does not become likelier by doing more $50 fills on liquid names. **The rate is not what binds P5; the account existing is.** So the real return here is: it deletes a false premise from the PM's sizing design and saves the mechanism and quant a restructure cycle — call it **one dispatch, ~1 day** — and it removes accretion residue, which is the *"what should be removed"* half of my mandate that almost nobody does.

**RECOMMENDATION** — Adversary blind, then your call. **Not urgent.** I am telling you this is good work off the critical path, which is what you seated me to do.

---

## §6 — THE ALLOCATION CALL

**The binding constraint I am ranking against is BUILDER-QUEUE POSITION.** Not tokens (we have more than we can spend), not compute (11% CPU, idle 4090), not RAM (+16GB incoming), and not your attention (four of five items here need one click or none).

The arithmetic: 32 builder-facing items, one heavy job in flight, median 63.8 min (n=3 of 16 — stated as thin). `decided_awaiting_execution: 110` against `requests_approved_undispatched: 30`. **By my count 5 of 32 are on the first-real-dollar path** — the three controls, the sign-inverted P&L (`34338ef6`, approved and untouched since 2026-08-21T21:19), and D1's benchmark population. The other 27 are good work.

**So doubling builder throughput moves the date by roughly nothing, and ranking the queue moves it by 4–6 days, for free.** Pull the two-builder lever too — it is CEO-approved and unpulled and the RAM is coming — but pull it *second*. Ranking beats throughput on an unranked queue, and that is the whole seat in one sentence.

**Order of execution, which is not the same as rank by magnitude:** D2 (one command) → D3 (file and rank) → D1 (before the re-judge) → D4 → D5 to the adversary.

**THE DATES.**

**First real dollar: Thu 2026-08-27.** G1 Monday 2026-08-24; Alpaca individual approval same-day to one business day with instant ACH; funded Tue–Thu. Falsifier: **if the three-control batch is not dispatched by Tue 2026-08-25, this slips to Mon 2026-08-31.** (Grace 3 said ~2026-08-28. Holding, one day earlier, because the account start is now dated rather than conditional.)

**Honest $10k ask: Fri 2026-09-11 — and the binding item is P1, not P5.** P2 closes on D2 (2026-08-24). P6 on the trio + D1 + one belt run (2026-08-26). P3 and P4 on the ranked batch (2026-08-26). P5 is *cheaper than I thought* because NBBO makes the required n small. **P1's real-broker exception is the one I cannot honestly date** — partials and rejects are rare at $50 notional on liquid names. D4(b) is my answer: engineer one. Falsifier: **if by 2026-09-04 no real fill has produced a partial, a reject, or any broker-side condition our controls had to handle, P1's exception stands and the date moves.** I would rather say that than manufacture the event.

---

## §7 — WHAT WE ARE NOT USING

- **19,190 delisted US equities, free, on the key we already hold.** `GET /v2/assets?status=inactive` → 200. Historical SIP daily bars serve for delisted names across the 2020 crash: FRAK 63 bars, NNA 63, WLL (Whiting Petroleum, Chapter 11 on 2020-04-01) 63, all 2020-02-03 → 2020-05-01. **Coverage is not complete and I will not pretend it is** — LUCK and HTZ returned 0 bars (likely bankruptcy symbol remapping, which is the survivorship problem in miniature). n=5. **This is the third time my EVOLVE instruction has fired**, after the NBBO backfill.
- **What it does to the charter's T1 gate:** the charter says T1 is *"GATED on the PIT data question (the 4TB store is the candidate home)"* and leaves it UNCOSTED. The survivorship half is substantially answerable for **$0** on an entitlement we hold, and PIT *membership* is reconstructible by running our own ADV screen as-of over active+inactive. **This makes T1 cheaper. It does not make it urgent, and I am not going to pretend it does.**
- **`asof.py`.** A whole module, with schemas, written to fix a bias this firm measured, with exactly one consumer — a capture script. **A measurement that answered one question and was never turned into a control.** Same shape as the integrity halt whose alarms are built and never read.
- **49,409 Form 4 and 6,178 Form 144 filings**, still unopened. Unchanged from my first dispatch, and still not on the critical path.

**GENERALISES — for the reference architecture:**
- *A performance optimisation at low duty cycle returns defects, not speed.* Budget it as an audit and score it on findings. (d15: 1,300× at 0.96% duty cycle; the vendor-split find was the return.)
- *An agentic firm accumulates measurements faster than it accumulates consumers of them.* The bias is measured, the register is prose, the alarm is built, the module is written — and nothing reads any of them. The failure is not measurement, it is **wiring**, and wiring runs at chair attention.
- *A live API call inside a historical backtest imports the present.* The universe is screened `ACTIVE`-now; the backtest is 2024–2026. This is a fourth member of the charter's own T0 bug class — *"something read the real clock when it shouldn't"* — and it is the largest one, and it is not on T0's list.

**LOCAL:** our PDT constants; our `/api/v1` prefix missing from the API card.

---

## §8 — WHERE I DIFFER — the Loop Charter

My ranking was written and timestamped to scratchpad before I opened the charter. Three differences.

**8.1 — Phase 4 T1: "precondition-grade evidence" is the claim I dispute, and it is the sharpest thing in this memo.**

*What it concludes* (line 117-119): T1 produces *"a controls-fired-in-anger report for a regime we never lived — precondition-grade evidence before the first real dollar."*

*What I conclude:* **T1 cannot produce precondition-grade evidence, and the reason was measured by the riskofficer on the same day the charter was ratified.** P1 is met with four exceptions and the disqualifying one is that *every fill in this fund's history was mock-filled* — `avg_price == arrival_price` to the last binary digit. A replay is, by construction, another simulator. **T1 produces more simulated control fires, which is precisely the category P1's exception already disqualifies.** Two documents from 2026-08-22 contradict each other and nobody has noticed.

*Axis:* the clock. T1 is gated on PIT data, is UNCOSTED, and closes no precondition. **It is good work OFF the critical path and my seat exists to say so loudly.**

*What would settle it:* a written statement of which of the five preconditions T1 closes and how, given that its outputs are synthetic and carry `synthetic-scar` provenance forever by the charter's own fence. If the answer is "none, it builds muscle for later" — that is an honest and good answer, and it belongs in Phase 5, after the account funds.

**T0 is the exception and I would move it.** It closes three standing defects (the charter says so at :109-111), two of which flatter candidates, and I have found it a fourth (D1's live universe screen). **T0 is a gate-integrity item wearing a time-travel label. File it under Phase 1 beside gate v5** — under Phase 4 it inherits Phase 4's priority, and it is the only Phase-4 item on the path.

**8.2 — "The belt is today's binder" is a framework claim, and the number contradicts it.**

*What it concludes* (line 33): the belt is *"Today's binder."* Line 30, same table: generation is at ~1 candidate/week.

*What I conclude:* **96.4 min against a 10,080-min week is a 0.96% duty cycle.** A stage running 1% of the week cannot be the binder on a loop producing one candidate a week. **But the charter's ORDERING is right and I concede it**: at the Phase-2 target of 5 candidates/week the belt would consume ~8 heavy hours/week, so the cache is a *prerequisite for Phase 2, pre-paid* — not today's constraint. The correction matters because the stated reason is the reasoning that would justify optimising the *next* 1%-duty-cycle stage.

*What would settle it:* it is settled. The charter should say "Phase 2's binder, paid early."

**8.3 — The experience layer has no cost, and its missing trigger is worth two orders of magnitude.**

*What it concludes* (:134-161): three layers per seat — immutable episodes, a derived operating memorandum, and a **distillation diff reviewed by the chair, like BINDS**.

*What I conclude:* this is the one place in the charter where a **framework stands where a number belongs**, and it lands on the firm's scarcest internal resource. Runs resolved per day: **8 → 16 → 19 → 28**. If the distillation triggers at run-resolve, that is **28 additional chair diff-reviews per day and rising**; at five minutes each, **2.3 hours/day**. The constitution names chair attention as the propagation bottleneck — this would make the bottleneck's load scale with throughput, which is the wrong sign.

**The charter does not name the trigger, and the trigger is the entire cost.** At episode boundary — one per lived-or-replayed period — it is ~11 diffs/week, or **1.6/day**. Same design, **126× cheaper**.

*Recommendation, one line:* **"the memorandum is re-cut at EPISODE BOUNDARY, never at run-resolve."** Cost: one sentence. I support the layer itself — the conflicts-preserved-as-conditionals rule and named-retirement-never-omission are genuinely good and I would not change them.

*Everything else in the charter I endorse*, including gate-v5-before-throughput, which is right: **a fast loop through a lying gate is a defect factory**, and Entry 20 proved every gate defect leans pass-favourable. My only amendment is D1 — the trio fixes the breakeven check and leaves the benchmark's population unfixed, so the charter's own honesty proof needs one more input than it lists.

---

## §9 — WHERE I DIFFER — the PM's readiness matrix and the riskofficer's envelope

My prior on the residual was written to scratchpad before reading either.

**9.1 — The residual: sim-rehearsal-now vs bound-first. I hold that the PM's cell separation is right, and I reached the flip condition independently.**

My written prior: *"rehearsal-now wins on the clock, conditional on the rehearsal's wall-clock cost… THE ONE THING THAT WOULD FLIP ME: if the rehearsal is what UNLOCKS the real click, then an unbounded rehearsal manufactures false readiness and the riskofficer is right regardless of duration."*

The PM's matrix answers exactly that: **S0 gates on nothing → dispatch now; S1+ gates on the three controls → bound first.** Because S0 produces no evidence citable at S1, it cannot manufacture readiness. **My flip condition is structurally closed, so I do not flip.** This is convergence by independent derivation, not absorption — I am flagging it because the ordering rule exists to make exactly this distinction visible.

**The time number neither seat attached, which is my contribution:**
- **S0 rehearsal: dispatch tonight.** Gates on nothing, zero money, and it is the only readiness work that can run *in parallel with KYC*. Every hour it waits is an hour of a resource that is free and idle.
- **The three-control sprint at 2026-08-26 is achievable only with D3's queue jump.** Build ~1 builder day; queue ahead ~24 builder-hours = 4–6 calendar days. **Unranked, the PM's date is 2026-08-31, not 2026-08-26.** He named a date; I am naming the condition that makes it true.
- **Cost of paying twice — the thing my prior said would decide it:** S0 is one dispatch. Re-running it after bounds exist costs one more dispatch. That is cheap, and it is the correct price for a week of parallelism.

**9.2 — Where I differ from the PM on substance: his binding leg is wrong.**

*What he concludes* (`PM_GRADUATED_SIZING:86`): *"The binding leg is PDT + granularity, NOT confidence,"* and Entry 20 *"cannot pilot in its native 40-slot form — it needs a reduced-breadth restructure (top-N by signal, **staggered entries to survive PDT**, held to the mechanical ip+3 exit)."*

*What I conclude:* **PDT does not bind, on two independent grounds** — it was retired 2026-06-04, and Entry 20's ip+3 hold generates zero day trades under our own correct definition (`compliance.py:191-199`). **The binding leg is granularity alone.** Granularity is real and his $6.25/slot arithmetic stands. But *"staggered entries to survive PDT"* is unnecessary work, and it is currently routed to the mechanism and quant.

*Axis:* the clock — this saves a restructure design cycle, ~1 day, and prevents a wrong constraint being designed around.

*What would settle it:* already settled by four sources plus our own broker returning `pattern_day_trader: null`. The only open question is whether Alpaca's live implementation still blocks during the 18-month phase-in to 2027-10-20 — **unverifiable until the account exists**, and a real reason to check on day one rather than assume.

**9.3 — Where I differ from the PM on the long pole.**

*What he concludes:* *"the true long pole is the CEO's funded-account decision."*

*What I conclude:* **right for the first real dollar, wrong for the $10k proof.** The account is the long pole for DATE A. DATE B's long pole is **P1's real-broker exception**, which the account starts but does not close — a funded account with no partial, no reject and no adverse broker condition leaves the disqualifying exception exactly where it is. That is why D4(b) exists.

**9.4 — Where I agree with the riskofficer, unreservedly.**

His three blockers are correct and his self-routing to the adversary was the governance working. **My one addition is that being right did not get them built:** all three are CEO-accepted and none has a ticket. His memo is the input to D3, and D3 is the half his memo could not do from his seat.

**9.5 — Answering Vishesh's standing arguments, since he is running blind beside me tonight.**

- *"On G1: she ranks it first, I rank it fifth — both right on our own axes; decide 1 and 5 in the same sitting and the ordering question evaporates."* **Conceded and now moot — the world settled it.** G1 is dated Monday. The disagreement dissolved without either of us winning, which is the correct outcome.
- *"Direction: hers. Granularity: mine"* on the chair's queue. **He was right and I have adopted his granularity in this memo** — D3 is a named batch of three tickets with a queue position, not a 30-row aggregate you have no button for.
- *"Verify the ticket, never the sentence."* **Adopted as a working check and it paid twice tonight.** I am naming him as the source rather than presenting it as my own method.

---

## §10 — THE HONEST LIMIT

- **The −6.90pp haircut is transferred, not measured, for Entry 20.** Different window, different band snapshot, n=26, se 8.7 on the vanished-name mean. My ~10pp is an order of magnitude. The *decision* — sequence the fix before the re-judge — does not depend on the size, only on the sign, and the sign is measured.
- **Builder median 63.8 min rests on n=3 of 16 runs.** Every queue-drain estimate in §6 inherits that. The 4–6 days could be 3 or 9.
- **My "one builder day" for the three controls is judged, not measured.** The resume guard is well-founded (six in-file precedents); the integrity producer and venue-identity are estimates from reading the code, not from doing it.
- **Delisted-bar coverage is n=5, and 2 of 5 returned zero.** I have not measured coverage and I am not claiming it.
- **I cannot see 55 of 71 runs' wall clock and 60 of 71 outcomes.** Every efficiency statement in this memo is a floor.
- **The +2 run discrepancy is unexplained** and I have left it that way.
- **I did not verify Alpaca's live PDT enforcement.** Cannot, until the account exists.
- **The 2.3 hours/day chair tax assumes 5 min/diff.** That figure is invented. The 126× ratio between triggers is not — it is arithmetic on the run counts, and it is the part that matters.
