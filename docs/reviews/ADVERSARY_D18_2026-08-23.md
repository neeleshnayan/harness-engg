# Adversary - D17+D18 whole-branch blind RE-review, 2026-08-22 late UTC

**Verdict: SURVIVES, whole branch (56d450a -> b6ea612) - MERGED by the
chair after clearance (1863 green; the drift alarm fired its true message
on live state within seconds).** Both D17 kills verified CLOSED BY
EXECUTION - the unmodified killing probes re-run and flip. Four fresh
attacks came back empty (no surviving drop-path incl. the expiry-refresh
attack; the report-vs-judged hunt found only a microseconds ts asymmetry;
the snapshot-staleness hazard is structural with zero live instances; no
frontend consumer of venue_drift exists). The disclosed annotation-set
loosening surface is REAL disclosure (a test demonstrates the hazard in
the open + a shrink-only pin). Two residuals ticketed, neither a blocker:
R1 the census scanner is blind to 2 of 3 live Event-construction shapes;
R2 the judged-vs-report pin is one-directional (the report adds a
rule-read `ts`). The adversary's own near-miss recorded: it nearly killed
on the census hole and refused - the runtime defect was fixed and a false
KILL costs five surviving items.

**Primary record: run `run-adversary-d18`; STATE in
`.claude/state/adversary.md`. Residuals ticket: 28cc60e2. The
drift-severity signature is on the CEO's desk.**
