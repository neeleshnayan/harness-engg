# Adversary — D32 gross-exposure repair, blind re-review, 2026-08-24

**VERDICT: SURVIVES — merge cleared. Both D29 grounds CLOSED by execution;
the certified rf work re-derived to 6dp (max deviation 2.73e-6 over 12
cells, D29's figures exactly); 13 modules byte-identical; zero loosening
found. Sixth kill→repair→clear loop.** Filed verbatim by the chair from
`run-adversary-d32`; probes `scratchpad/adv32/` (promoted: `ceiling.py` —
the exposure-rule instrument; `departure2/3.py` — the gap-tolerance floor
+ reachability pair, reusable on ANY coverage/staleness tolerance;
`reader.py` — the chart-reader axis battery).

## Grounds

**G1 CLOSED**: 28/28 levered cells refuse ON THE CEILING against the real
engine chart shape run through the shipped reader (the reviewer's own old
probes were anti-models refusing on absence — classified per its EVOLVE,
then re-derived from the real producer); 16/16 unlevered controls still
measured, 2 pass — the bar is alive. `sharpe_advantage` suppressed on
every refusal. No production caller can override the ceiling.

**G2 CLOSED for every reachable shape**: probeF unchanged flips exactly
the two joint-truncation rows pass→fail. Residual bounded and named: an
adversarially phase-locked thinning can vouch 32.0% true coverage as 100%
(v5r2 was UNBOUNDED at 15.6% and falling); under i.i.d. correlated
outage, vouched ⇒ ≥85.9% true coverage, and nothing vouches at drop rate
≥0.20. Both realistic vendor shapes (tail-lag, interior outage) are
caught. A residual for the record, not a verdict.

## Eight residuals, each with direction and money (all $0, none blocking)

1. `gross_exposure`'s per-timestamp join is **fail-OPEN on unaligned
   classifiable series** (reads 0.6 for a true 1.2 when two security
   types sample on different clocks). Reachability ZERO today: 108/108
   real charts carry only `Base`; Long/Short stamps identical 108/108.
2. **`SESSION_SPAN_TOLERANCE_DAYS = 5`'s written reason is falsified by
   the fund's own feed** (SPY 2001-09-10 → 2001-09-17: a 7-day session
   gap, NYSE closed after 9/11). The VALUE errs safe (false non-vouch →
   stricter calendar fallback); the provenance sentence is wrong. A
   threshold's reason is auditable independently of its number.
3. `_curve`'s refactor narrowed an except tuple — `OverflowError` now
   leaks on a 400-digit JSON integer. Diff except tuples on every
   helper extraction.
4. `max_long` reports 0.0 on an absent long series — absence-as-zero in
   a reported field.
5. The refusal path still publishes `sharpe_advantage_raw` (+17.80 on
   the 1.05× book) beside the refusal.
6. The version note OVER-ATTRIBUTES its own cost: all 55 stored results
   already refused premia at base (`premia_inputs = None`), 55/55
   identical base↔head — self-penalising misattribution, not a loosening.
7. Census drift noted (110→113 result files); the load-bearing half
   re-verified: 108/108 with statistics carry Exposure.
8. **The intraday surface is SMALLER than disclosed, measured**: the
   chart samples daily at 00:00 ET (71,328/71,328 stamps), so overnight
   leverage IS caught; only intraday-only leverage evades — and it earns
   no overnight cash accrual, which is the gift the ceiling blocks.
   CANNOT-TELL on LEAN's intraday accrual without a container run.

## Failed attacks (nine, named)

The three anti-model probes (classified); the `==1.0` boundary
(round-to-6dp admits true gross ≤1.0000004, gift ≈1e-7 Sharpe — empty);
caller-side criteria override (unreachable); test weakening (0 removed
unreplaced); alpha identity 0/55; premia store identity 55/55 identical;
four reader shapes fail closed; realistic G2 reachability empty at
q≥0.20; the version note names the widening a widening and routes it.

**Chair note at filing**: the policy question (refuse vs price financing)
was decided under the CEO's explicit delegation before this review filed —
THE REFUSAL STANDS, reopening trigger recorded (a gross>1.0 proposal with
a named financing counterparty + the intraday answer in hand; then
blind-first, CEO click). The three repair-required residuals and the two
documentation corrections fold into the D34 spine batch.

**Primary record: run `run-adversary-d32`; STATE in
`.claude/state/adversary.md`.**
