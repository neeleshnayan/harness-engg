# Adversary — D22 desk engine (spine half) blind review, 2026-08-23

**VERDICTS: surface (a) — the pre-guard superseded-row refusal —
SURVIVES (the "can only refuse, never admit" claim CERTIFIED across 11
executed paths; protected guard chain proven AST/byte-identical; two
non-loosening defects). Surface (b) — the fail-open — KILL on three
executed grounds. Auto-hygiene SURVIVES three layers deep (154
hand-built combinations + 9 malformed proposals + the structural
argument: the event type itself can only produce `resolved`). BUNDLE
KILL, merge-whole-only — a repair list on a good engine.**

## The (b) kill, three grounds
1. **The disclosure that justifies failing open does not exist**: the
   promised `supersession_readable: false` appears once in the repo —
   inside the sentence claiming it. An approval taken during an outage
   is indistinguishable in the record from a verified one.
2. **A LIMIT is a silent off-switch**: `by_target()` truncates at 1,000
   edges (executed: at 1,001 the R37-specimen brake vanished while the
   store reported healthy). The unguarded POST makes the flood
   immediately reachable; the organic path is monotone.
3. **Validate-stripped / store-raw**: an edge filed with a trailing
   newline or wrong case is accepted, listed, and blocks nothing.

## The bundle kill's second ground — the half-shipped contract
Routing v1's 422 enforcement, run over the LAST DAY of live traffic:
**16 of 17 runs recorded today would be rejected, across eight seats**
(the lone survivor was chair-composed). The companion (seat protocols /
run-record format) lives outside the builder's write scope. Ships gated
behind a version flag until the chair lands the companion.

## Also found on (a), neither loosening
The 409 appends NO event — the fund's first silent approval refusal
(base recorded `ApprovalRefused` for non-allowlisted attempts; the
precedent `_guard_mark_sanity` records its 409) — and it hands lineage
to callers who never passed the allowlist. And the fail-open policy is
decided by CACHE WARMTH: a cold-cache outage 500s the approval path
(the exact thing fail-open was meant to prevent) while a warm cache
fails silently open.

## For the CEO (governance-gap, on the desk)
`POST /fund/desk/supersessions` + `/retract` carry no allowlist, no
echo — the brake in front of the approval path can be added/removed by
any caller reaching the spine. Consistent with the desk family's
unguarded filing, and no money moves without the unchanged guard chain
— but whether a control ON the approval path may sit on an unguarded
channel is a human's call.

## Failed attacks, named
Guard-chain tampering (nine money modules byte-identical); admission
(11 paths, zero); hygiene (unbreakable); test weakening (zero removed,
2088 collected exactly); display caps publish shown/total; merge
overlap empty. The adversary's own near-miss (sixth in eight) reported:
nearly filed the routing kill on the STORED rows — the store normalises
on write; re-derived from values.

**Primary record: run `run-adversary-d22`; STATE in
`.claude/state/adversary.md`; probes `scratchpad/advd22/` (probeB5 =
the door-predicate-over-live-traffic instrument, reusable). Repairs are
all probe-verifiable by re-running unchanged.**
