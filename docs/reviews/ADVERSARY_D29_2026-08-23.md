# Adversary — D29 premia-rf repair, blind re-review, 2026-08-23 (late)

**VERDICT: KILL (merge), narrowly grounded on UNFINANCED LEVERAGE — and the
D23 rf ground is CERTIFIED CLOSED by execution. Fifth kill→repair loop;
first where the repair is certified correct AND the branch still dies, on a
hole the repair's own notes deny.** Filed verbatim by the chair from
`run-adversary-d29`; probes `scratchpad/adv29/` (promoted to the instrument
shelf: `probeD.py` the financing instrument, `probeF.py` the truncation-
shape characteriser, `base.py` the pinned-feed rf fetcher).

## The certification (say it first): D23's ground is closed

Falsifier (a) met exactly — probe3's 16 zero-skill cells collapse to max
|adv| 0.0297 (< 0.05), 11/16 passing → 6/16, and the shipped
`sharpe_advantage` reproduces the reviewer's independently computed true
excess advantage to **4 decimals on all 12 measurable cells**. Realised-rate
table 4.05/4.35/4.57/3.25 %/yr reproduced. Alpha identity: 55 stored
results, 0 diffs. **The rf work is correct and must not be rewritten.**

## G1 — THE KILL: subtracting rf closes the carry channel only for gross ≤ 100%

LEAN's default charges no margin interest (`NullMarginInterestRateModel` in
`DefaultBrokerageModel`), so a levered book's excess is `Σwᵢrᵢ − rf`, not
`Σwᵢ(rᵢ − rf)` — a free gift of exactly **(1 − 1/G)·rf/sd**, monotone
INCREASING in cash weight. Executed on the fund's pinned feed
(`adv29/probeD.py`): 1.25× (25SPY/75BIL) vs SPY **passes all four belt
windows at +0.153..+0.239 with a financing-charged advantage of 0.0000**;
3.0× scores +2.49..+3.92; the degenerate 1.05× BIL book scores +11.4..+18.1
at 0.01% drawdown (`probeB.py`). The margin is 0.0, so +0.153 decides — and
the largest GENUINE advantage the fund has measured is +0.054. Both
benchmark-relative criteria clear: the Sharpe leg on the gift, the drawdown
leg *because* the book is cash-heavy.

Three artifact claims falsified: "the movement is total" (gate.py:625-631);
"INVARIANT to the cash weight" (true only within gross ≤ 1.0 — one step
across G=1 the dependence inverts); "those false passes are selection
noise" (every levered construction is deterministic). The disclosure that
exists ("a financing SPREAD is unmodelled") names ~25% of the measured gap
— the BASE rate dominates. The invariance test sweeps w ∈ (0.1..0.9),
never crosses 1.0, and its fixture charges rf on the borrow the way the
engine does not. **No test in the branch mentions leverage.**

Money weighed: $0 today (all 16 algorithms ≤ 0.95 gross, no margin model,
no premia candidate, and the live repo has NO premia bar — `grep -c
PREMIA_VERSION` → 0). A merge condition, not a fire — but merging IS the
decision to ship the bar, and the verdict payload carries no gross-exposure
field, so a levered book's borrow is invisible to every reader. And the bar
exists to admit VOL-SCALED books, which lever by construction.

## G2 — supporting: the coverage denominator degenerates under correlated truncation

The session denominator is the UNION of the bar's and the cash leg's dates;
both come through `fetch_daily_bars` (leanrunner.py:1346, :1746), so a
vendor tail-lag truncates both together and the test degenerates to
common/common — **15.6% coverage reads as a strict majority and PASSES**
where v5r1 refused (`probeF.py`). Bar-only truncation: versions agree
exactly; rf-only: v5r2 tightens; only the joint case loosens, unboundedly.

## Clearing conditions (verbatim)

G1: capture gross exposure in the premia payload AND refuse a premia claim
above gross 1.0 in the same fail-closed shape as `excess_measurable` — or
charge financing in the engine (re-run probeD: the shipped column must
collapse onto the financed column). G2: check the cash leg's dates span
`s_dates[0]..s_dates[-1]` before reporting `strategy_sessions`; fall back
to the stricter calendar count. Plus: correct the two falsified docstring
sentences; widen the invariance test past w=1.0 with an engine-faithful
fixture. **The rf work is preserved intact through whatever fix lands.**
The refuse-vs-engine-price SHAPE choice is routed to the CEO as a
control-layer judgement (policy question on the desk); the refusal itself
is a fail-closed tightening.

## Failed attacks, named (ten)

12 modules byte-identical (AST-with-constants — and the reviewer fixed its
own scanner: `AnnAssign` was a blind spot in the old copies); CRITERIA/V2/
V3 unchanged; 55-result re-judge 0 diffs; `"constant"` basis reproduces
v5r1's rf arithmetic 20/20; 6-of-6 fail-closed battery; paired Dirichlet
census +0.0..+0.9pp (unchanged-within-noise claim reproduces); the
disclosed 2021+ cash-heavy loosening independently reproduced (14.5→31.8%
vs stated 15.4→29.5% — honest); SHV-vs-BIL basis attack EMPTY; judge
endpoint backward compatible; the one removed test replaced, both
loosening tests genuine disclosures.

## The reviewer's own double near-miss, reported by the seat

probe3 unchanged CRASHED (KeyError) and probe8 unchanged printed "0.0%
false pass" — a false KILL and a false SURVIVES in one minute, both
anti-models (no rf fetcher = they measured the new fail-closed path). The
CALL-vs-MODEL discipline caught both; every figure above comes from the
re-derived harness (`adv29/base.py`).

**Primary record: run `run-adversary-d29`; STATE in
`.claude/state/adversary.md`.**
