# Adversary — D19 blind review (benchmark population + gate v4.3 pair), 2026-08-23

**BUNDLE: KILL (merge-whole-only ships an acceptance-criterion breach).
Item 1 (739b5ac9, benchmark population): SURVIVES + one repair required.
Item 2 (58c4fff5, the gate pair): KILL as specified — two breached hard
criteria, both disclosed by the builder, neither the builder's to waive.**

## K1 — net loosening on the window the pair ships to

Measured on the SHIPPED factory fixed point + SHIPPED gate rule, paired
common random numbers, n=6,000, SPY's own calendar: zero-skill false-pass
**3.33% → 5.00% (+1.67pp ± 0.31pp, 5.4σ)** on the deep-floor window —
which ships TODAY to the two lookback=2000 algorithms including the live
`monthend_rebalance_flow` family. The spec's hard criterion: "the pair
must NOT be a net loosening on any window," with loosening defined by the
spec's own parenthetical as false-pass. The adversary attacked its own
kill and reported the counter honestly: power also rises 22.6%→32.5%, so
likelihood ratio is FLAT within noise (6.80→6.50) — the pair buys power
WITH false passes, a trade a CEO may want and must take himself.
Severability measured: commit `24e756a` (fold scaling alone) cannot
loosen anything on any window (`required = max(anchor, scaled)`; floor
unchanged there); the kill costs one commit. A twelve-fold configuration
(2.90% FP / 40.7% power) strictly dominates today but is unreachable
under the shipped `window_for` reach-back and `max_folds=max(min_folds,6)`.

## K2 — behaviour-identity claim false for holds 16/17/18 (repair, not kill)

Three shipped statements claim identity "for every hold the generator
produces"; the acceptance test parametrizes 8 holds of an unbounded
integer domain. Widening the artifact's OWN test to `range(1,70)` — zero
other edits — fails on 16/17/18 in 0.21s (5 folds required where v4.2
required 4; failure sentence gains the density suffix). Direction is
TIGHTER, so repair-required: narrow the claim or enumerate the domain.

## Item 1 SURVIVES — the attacks that failed, named

No unlabelled benchmark can leave (`_add_benchmark` single producer, every
exit labels or pops); absence is not zero (dead-DSN test returns
`delisted_priceable: None`); no DDL or live-DB writes (SELECT-only path,
autouse fixture); the ETF type-coverage trap is guarded (verified live).
Also clean: CRITERIA byte-identical (AST), scope exactly 15 files with all
protected surfaces untouched, 1863→1939 tests with ZERO lost, no ratchet
path shortens any window, every counted claim reproduces exactly.
**The one repair**: `population_report` sets `listing_asof_applied: true`
even with ZERO names judged, and `gate.py:602-610` drops
`unjudgeable_by_snapshot` from the stored verdict — reachable today (34 of
41 candidates use the 2025-01-01 holdout, the only snapshot's date).
Money at risk today $0 (no threshold reads the label).

## Non-blocking findings

1. A THIRD copy of the retracted measured table survives in
   `tests/test_fold_scaling.py:16-17`.
2. `effective_history_floor` anchors reach to `holdout.test_end` while
   container URLs trail the wall clock — on a backdated holdout the window
   opens deeper than the data path and `folds_before_data_path_reach`
   reports 0: an absence rendered as zero in a brand-new counter. Tighter
   direction; repair with the rest.
3. `scripts/null_audit.py` reads the raw floor with no ratchet — the
   gate's calibration instrument would stop running the gate's geometry.

## For the chair, on the record

The `min_walkforward_folds` register entry carries a BLOCKING review
trigger ("ANY extension of history past 2024-02-26") whose `trigger_spec`
is `[]` — this diff fires that trigger and discharges it in its own prose,
and the register can record neither. The free-text-trigger defect the
constitution names, first time at the control layer.

**Primary record (verbatim): run `run-adversary-d19`; STATE in
`.claude/state/adversary.md`. Probes: `scratchpad/adv19/` (fp2.py is the
reusable paired-FP harness for ANY gate-rule change). The CEO decision —
accept the power-for-false-pass trade as a versioned amendment, or sever —
is on the desk.**
