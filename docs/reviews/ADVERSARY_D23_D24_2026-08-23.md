# Adversary — D23 premia gate (fresh blind) + D24 desk repairs (re-blind), 2026-08-23

**VERDICTS: D23 `builder/d23-premia-gate` — KILL on the premia criterion →
BUNDLE KILL, merge-whole-only (the capture-only work and the alpha-identity
claim SURVIVE, loudly: eight attacks on them came back empty). D24
`builder/d24-desk-repairs` — SURVIVES: every D22 kill ground closed by
execution; fourth consecutive kill→repair→clear loop on the desk engine.**

Filed verbatim from run `run-adversary-d23-d24` by the chair. Probes:
`scratchpad/adv23/` (probe1–9, gate_base, astdiff_d24, d24_store,
d24_routing + pinned bar JSONs). Chair spot-check at resolve: `probe3.py`
re-run, output reproduced exactly.

## THE D23 KILL: the premia bar certifies zero-skill cash/beta blends

`gate.py:605` sets `premia_rf_stress_pct: 4.0`; `gate.py:845-846` runs the
inequality at rf=0% and rf=4%; margin 0.0 (`gate.py:587`).

**1a. The constant is BELOW the cash rate the belt's own windows pay**
(probe1, pinned alpaca feed): full window BIL 3.26%/yr, **belt 700d 4.07%**
(11 of 16 fleet algorithms use 700d), **belt 900d 4.37%**, **2023+ 4.59%**
— against the shipped 4.00%. The 4.0 was "rounded UP" from BIL 3.97% on
ONE window (`docs/GATE_V5_ROUND5_MEASURED_2026-08-21.md:52`); the belt does
not run on that window. On three of four, the stress is softer than the
realised rate — the one condition under which a cash tilt survives it.

**1b. Executed — what passes** (probe2, bar built exactly as
`leanrunner._add_benchmark` builds it): 20% SPY / 80% BIL **PASSES on
700d, 900d and 2023+**; 40/60 likewise; 100% T-bills vs EW(SPY,QQQ,IWM)
passes on two windows. Controls behaved (the bar itself fails; 2× levered
bar fails on drawdown).

**1c. The decisive arithmetic** (probe3): twelve of sixteen zero-skill
cells PASS while the TRUE excess-Sharpe advantage — computed with the
REALISED BIL series per the constitution's own definition — is between
−0.0004 and +0.03 in every one. E.g. belt 700d, 20/80 blend: true
advantage −0.0004; shipped advantage +0.7208 at rf=0, +0.0110 at 4%.

**1d. The fund already measured that this remedy shape fails.**
`GATE_V5_ROUND5_MEASURED_2026-08-21.md:88-96`, verbatim: *"A plausible
static assumption is not safe"*; *"the risk is static vs realised, not
which bill fund."* D23 ships a static assumption and cites that document
for the number.

**1e. Nothing else catches it**: once `must_beat_benchmark` is replaced
(`gate.py:1099`), ZERO of the 13 remaining criteria are benchmark-relative
for a premia claim; all are absolute or self-consistency checks a rising
market plus T-bill carry satisfies, and the breakeven leg reads
total_return, which carry inflates.

**1f. Zero-skill false-pass rate in the belt's own geometry** (probe8,
1,000 Dirichlet portfolios/cell): **22.7–27.8%** at 700d/900d (rising when
BIL joins the universe), vs the 18.2% the diff discloses — disclosure
honest, number measured in the wrong geometry.

## D23 non-loosening defects (report, not merge-blockers)

1. `POST /fund/lean/gate/judge/{job_id}` (fund.py:2548-2566) accepts no
   `claim_type` — a premia candidate re-judged there silently reverts to
   the alpha bar and is stamped v4.3. The factory path IS wired.
2. `premia_inputs` coverage divides trading days by calendar days:
   0.67–0.69 on all 15 real specimens, ~19pp of slack in the majority
   check, and a reader cannot tell whether anything is missing.
3. "Byte-identical alpha verdicts" is false as a literal dict claim —
   `checks` gains three keys on every alpha verdict; the DECISION is
   identical (54 of 54 stored results: 0 diffs in passed/failures/version).

## D23 failed attacks, named

CRITERIA/V1/V2/V3 compare True base↔head; only `evaluate` changed in
gate.py, zero symbols removed; 54 stored results re-judged 0 diffs, 0
crashes; fail-closed on absent premia inputs (0/54 measurable → failure
sentence); unknown claim type fails both directions; drawdown sign attack
empty; clock invariance holds (248–252 obs/yr after intersection); claim
shopping on the REAL population is 1 flip of 15 (7 clear alpha, 7 premia,
6 overlap) — **the kill rests on the constructed adversary, not
population drift**; test weakening NONE (1,227 lines added, 0 modified or
deleted).

**CANNOT TELL**: full suite not run by the reviewer (host RAM 1.45→1.06 GB
during the dispatch). [Chair note at filing: the builder's addendum later
discharged it — 3488 green on branch AND merged tree; merge gate
FAIL-by-routing, 0 forbidden.]

**What would change the verdict**: (a) the stress rate becomes the
REALISED cash return over the candidate's own window, read from the feed —
re-run probe3 and every cell should collapse to |adv| < 0.05; (b) a
benchmark-relative criterion survives for premia that the cash blends
fail; (c) a measurement that the belt cannot produce windows where
realised cash exceeds 4.0%.

## D24 re-blind: SURVIVES — per-ground table

| # | D22 kill ground | status |
|---|---|---|
| (b)-1 | disclosure did not exist | **CLOSED** — on response + DeskRequestApproved payload + tri-state on recommendations; probeF/probeA2 verified |
| (b)-2 | silent 1,000-edge truncation | **CLOSED on silence** — probeA now dies on `SupersessionsTruncated` at 1,001; `_page` fetches limit+1; residual named below |
| (b)-3 | validate-stripped / store-raw | **CLOSED** — real-store re-derivation: whitespace/case refs store canonically and BLOCK; migration rewrites legacy rows, names the unparseable one |
| (a)-i | silent 409 | **CLOSED** — every refusal path writes ApprovalRefused (fund.py:2340-2360) |
| (a)-ii | lineage leaked pre-allowlist | **CLOSED** — guard before lineage (fund.py:1741-1743) |
| (a)-iii | cache-warmth fail policy | **CLOSED** — construction inside the try; cold cache degrades disclosed, no 500 |
| bundle | routing 422 half-shipped | **CLOSED** — dark behind flag; 0/24 gated, 16/24 flag-on, reproduced three independent ways |
| — | unguarded supersessions POST (out of scope) | unchanged, **not widened** (AST: not in changed set) |

Nine money modules byte-identical; three removed tests each replaced by
tests asserting the corrected behaviour (net +21 functions) — no
weakening. The artifact's own three probes reproduce exactly.

## D24 residuals (none blocking; for the record and the riskofficer)

1. A 1,001-edge flood now takes EVERY brake off LOUDLY where before it
   took one off silently — strictly better against the targeted attack
   that was the kill; worse in reach for organic growth. The CEO owns that
   trade (it is the same governance item as the unguarded POST).
2. `supersession_readable` is written 10× and read NOWHERE — the
   riskofficer must query it explicitly.
3. `validate_routing` has zero production callers and returns `[]` for an
   opt-in caller the door will 422 — a pre-flight helper that lies; fix or
   delete at next touch.
4. `ApprovalRefused` gains a producer at the unguarded
   `decide_recommendation` (caller-supplied actor) — priced at $0 today
   (type appears 3× live); riskofficer filters on `payload.guard`.

## The reviewer's own near-miss, seventh in nine — reported by the seat

probeC section E (hand-built map) printed "still broken" against a store
that now canonicalises on write — an exact model of the defect became an
exact anti-model of the repair. Caught before filing; EVOLVE accepted into
the seat file (classify probes: CALL vs MODEL the repaired layer).

**Primary record: run `run-adversary-d23-d24`; STATE in
`.claude/state/adversary.md`. Reusable instruments: `adv23/probe3.py` (any
future premia rule), `adv23/probe5.py` (gate re-judge identity harness),
`adv23/astdiff_d24.py` (repoint and run).**
