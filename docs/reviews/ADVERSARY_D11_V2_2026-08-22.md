# Adversary — D11 v2 blind re-review: SURVIVES / SURVIVES

**Filed by the CTO chair 2026-08-22 from run `run-adversary-d11-v2`. The v2
repair of the fund-mode diff the adversary itself KILLED as D11. Nothing
merged — this is the event store, so the crossing is the CEO's, informed by
this. Chair note at the end.**

## Verdict: SURVIVES (ClarkHarness `v2-d14` @ `ddc05a2`) · SURVIVES (KryptonPay @ `82496582`)

Every claim executed, not argued. All eight D11 kills (K1–K8) verified
genuinely closed. Independent full suite reproduced: **1694 passed, RC=0**
(captured via a separate file, not a pipe); repaired-area subset 204 passed.

## The K2 override — the builder was RIGHT; the adversary confirms its own remedy was wrong

The sharpest result, and the adversary folded the code itself because it was
its own spec being refused. Two `EXIT_RULE_SET` on one key, run against the
real module:

- `_fold` (`exitrule.py:183`) sets `superseded=True` on the **surviving,
  governing** rule — it marks *revised*, not *dead*, and the survivor carries
  the latest threshold.
- `enforce()` skips **only** `triggered_at` and `overridden_at`
  (`exitrule.py:298,303`), never `superseded`. So a coverage filter on
  `superseded` would disagree with the enforcer — the exact defect K2 was
  raised about, pointing the other way.
- Decisive case: re-committing a fired rule clears `triggered_at` and sets
  `superseded=True`. The original K2 spec's "not-superseded" third would have
  scored that *restored* rule dead forever — a false alarm on the one
  mechanism that restores a fired exit.

**"My K2 spec was two-thirds right and one-third wrong. The builder correctly
implemented the two-thirds and refused the third."** The INTC exposure the
adversary attributed to supersession is still caught — by the ownership key —
and the full $1,165.44 of unmanaged positions is reported.

## The other seven, verified by execution

K1 test ledger → `krypton_fund_dev`, no test truncates it, new regression
guard. K5/K5b prod locks: every path (`resolve`/`activate`/`activate(force)`/
`pg_dsn_for`) refuses `ProdLocked`; `allow_prod` deleted (zero callers);
`reachable` reads the same line that refuses; a mock connector named `"alpaca"`
refused for the real-broker mode by `isinstance`. K4 `CashReconciled` deleted
(zero ever written), five more producer-less NAV folds named in a shrink-only
guarded budget. K3 partial release preserves the reduced holder pro rata
(SPY's survivor stays in its sleeve). K6/K7/K8 closed.

## Independently verified, and one scoping note for the chair

`autopolicy.py` and `gate.py`: **byte-identical** base→HEAD — the auto-approval
envelope is untouched. Scoping note the 2-file sensitive flag under-reports:
the diff also edits the NAV-fold projections (`nav.py`/`positions.py`/
`strategy.py`) — event-store-adjacent Tier-3 surface — and the adversary read
them: the K3/K4 corrections plus a reconciliation-vs-performance split in
`nav.py` that never reads broker equity as NAV. The invariant holds.

## Two things for the human

1. **Merge the whole branch (`ddc05a2`), never a prefix.** Commit 2
   (`5ecebfa`) ships the K2 defect; commit 11 (`ddc05a2` = HEAD) fixes it. A
   prefix ending before HEAD ships the loosening.
2. **`confirmEcho` collides `alpaca-paper`/`alpaca-prod` → `alpaca-p`**, and a
   test asserts the collision — but the adversary verified this is *disclosure,
   not a blessed loosening*: the switch endpoint selects on `req.mode`
   (`fund.py:741`), never the echo, and prod is server-locked. Harmless today;
   **must go on the prod-unlock checklist** — the day the server lock opens,
   the echo becomes the last human-readable confirmation and it cannot tell
   real money from paper.

## CANNOT-TELL, stated

Postgres was unreachable at review time (5433 OperationalError), so the *live*
existence of `krypton_fund_dev`/`prod` is unverified — immaterial, because no
test destroys it and boot creates it, but flagged rather than inferred.

---

# CHAIR NOTE

This is the candidate-chain and blind review working in full: kill → repair +
refute-the-remedy → reviewer confirms it was wrong, all by measurement. The
chair spot-verified the byte-identical guard/autopolicy claim and the
merge-whole hazard. **v2 is adversary-cleared and does NOT merge on the
adversary's verdict or the chair's — it touches the event store, so the
crossing is the CEO's explicit decision, exactly as envelope v4 was.** It
fixes a live defect (the spine returning `venue:alpaca` with
`orders_are_real:false`) and no money moves on merge — the whole book is still
paper. Presented to the CEO's morning desk: merge the whole branch, or park it
whole.
