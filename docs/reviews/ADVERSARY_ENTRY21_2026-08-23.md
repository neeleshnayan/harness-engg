# Adversary - Entry 21 (Treasury auction concession) + the breakeven-floor challenge, 2026-08-22 late UTC

**Verdicts: KILL / KILL - the chain's cheapest kill ever (zero containers
spent).** Entry 21: the return series is REAL and independently reproduced
(+6.00%/yr vs claimed +6.95) but TWO-THIRDS is a day-of-month calendar
pattern (tdom fixed effects AND a matched-calendar control agree; a
zero-auction-information tdom rule earns +4.82 of the +6.00); all three
mechanism discriminators fail under correct controls (the proposal's own
ladder omitted the 3-YEAR - the counterexample; size monotonicity vanishes
within year-by-term cells; the published dip-AND-RECOVER shape is absent);
and the money dies independently - the 19.7 bps breakeven is 37.3 in
2008-13 (in-sample vs the 2013 paper) and 8.7 post-2013, UNDER the 10.0
floor on the only out-of-sample era. Five failed attacks reported as
loudly as the kills (the 0/300 placebo is honest - and the wrong null;
w_hi genuinely unfitted; the EW-rebalance attack's first-ever empty
result; FOMC not a confound). The floor challenge KILLED as filed: its
central premise cites a CLOSED defect (tca.py:318-326 excludes latency
drift), the keyed quantity (half-spread) is unmeasurable with any data
the fund holds, and the remedy would not rescue its own named victim. A
five-condition safe path is specified for any re-filing.

**Primary record: run `run-adversary-entry21`; STATE in
`.claude/state/adversary.md`; six lessons carried to Ed in
`.claude/state/mechanism.md`. The matched-calendar control
(scratchpad/adv21/matched.py) is a standing instrument.**
