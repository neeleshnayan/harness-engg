# Ed — batch #3 (funnel cycle 8): the batch that closed the calendar lane
**Filed by the chair at resolve; operative summary — the full memo with all
kill tables is verbatim in run `run-ed-batch3`. Zero containers; zero
adversary tokens spent on this seat's output.**

**VERDICT: 0 filed, 6 constructions desk-killed or refused, every decisive
number Recount-verified (8 exact, 2 label defects corrected pre-filing).
The liquid-ETF calendar-anomaly lane is CLOSED BY MEASUREMENT, not
fatigue.**

## The kills, one line each (full tables in the run record)
- **K1 Macro announcement-day duration premium (the shelf flagship)**:
  sign WRONG (release-day TLT−SHY −2.17 bps/day, t=−0.53, n=576);
  negative in all four FE specs; bottom-tail of its matched-calendar
  placebo; every trailing rung negative; the equity variant reproduces
  the published day-of-month null (EGH) on our own feed. Citation
  discipline caught JLL's set excludes CPI. Revival condition carries its
  MDE (~23 bps/day — reachable, unlike E21's bar).
- **K2 Turn-of-month switch**: the adversary's own named re-entry path
  measured and FAILED (pre-declared window marginal t=1.38, rank 5/21;
  day −1 significantly negative); the Dash-for-Cash mechanism is pinned
  to T+3 settlement, which ended 2024-05-28 — T+1-era effect +1.0
  bps/day. The calendar-flows family goes fully dormant.
- **K3 Entry-20 reversal descendant**: direction right (t=−1.09),
  trailing-24m sign flip, MDE needs ~13 years of panel — SHELVED with a
  stated revival. Side-finding to Stan: the give-back is NOT currently
  biting Entry 20's live construction.
- **K4 Term premium as premia**: negative on every trailing rung — stays
  declared beta. **K5 Entry 16: recommend RETIRE** (modern BE 7.16 < 10,
  batch-#2 measurement; D6 landing no longer rescues it).

## THE SURVIVAL-RATE MEASUREMENT (the batch's second job)
**Pre-card denominator: 0-of-3 filings survived the adversary, ~160k
tokens per kill. This batch: 6-of-6 constructions killed at Ed's OWN desk
by the card's controls — zero adversary tokens.** The card demonstrably
converts review kills into free self-kills. Honest limit: whether it can
produce a SURVIVOR is unmeasured, because **the constraint moved
upstream: the in-house idea space on liquid-ETF calendars is
measured-empty. Generation is blocked on DATA delivery, not on looking.**

## Self-fanout v1.1, second outing: criterion met again
The flagship was redesigned and killed WITHIN the run on a research
worker's finding (the EGH day-of-month null reshaped the crunch spec
before it was issued) — a third measured mid-run catch. Subtree 523,747
tokens, caps respected, ledger reconstructs, foreground plumbing worked.
The Recount recut to spec v2 (own-seed Monte Carlo rank-direction
judging; label-and-convention verification; instrument facts as
reportable output).

## The CHALLENGE (routed to the CEO) and the forward path
**To dispatch rule 5 (Ed's generation trigger)**: three consecutive
batches (1, 2, 0 filings; 8 kills, all identification/harvestability
against unchanged data) show the marginal Ed batch against unchanged data
returns zero at ~525k tokens. Proposed: when the floor trips AND the
prior batch named a data blocker, route FIRST to the data-unblock
dispatch; Ed fires on delivery. Weekly clause + manual dispatch
untouched. **Data asks, ranked**: the 8-K item-code event panel (7,512
events, already extracted, needs shaping — dispatched by the chair at
resolve); CEF discount source check; FINRA short interest/FTDs.

**Instrument defects found**: the bars API `end_date` is EXCLUSIVE (card
corrected at resolve); the menu doc is stale (15 entries vs 26+ in the
record — chair housekeeping).

**Primary record: run `run-ed-batch3`; STATE + Recount spec v2 in
`.claude/state/mechanism.md`.**
