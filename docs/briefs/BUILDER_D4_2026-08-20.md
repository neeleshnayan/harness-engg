# Builder dispatch 4 — the accepted batch: safety, instruments, halt classes, CDO fixes

**2026-08-20, CEO-authorized ("good inputs, lets get builder on it").
Everything below is a CEO-ACCEPTED recommendation (validator run 8b863152,
CDO run-cdo-trial-1, riskofficer F4, the CEO's halt-principle blessing) —
scope is fixed; judgement calls inside scope are yours and must be named.
The 3D floor (CDO Deliverable B) is deliberately NOT here — it gets its own
dispatch so its acceptance criteria get undivided verification.**

## Bases (verify before any work — refuse a wrong base in minute one)

- ClarkHarness base: **bd4b30c** ("Concentration limit re-specified…")
- KryptonPay base: **e110b4c** ("CDO trial artifact filed verbatim…")
- Your worktree has been created wrong three dispatches running. Method
  that works (your own STATE): `git clone --no-hardlinks` each repo inside
  the worktree, `git -C <abs-path>` for all git ops (the bash guard rejects
  `cd X && git ...`), junction node_modules, copy `.env.local`. Run
  `git log -1` in each clone and CHECK it matches the bases above.
- Deliverables: one bundle + patch PER REPO, tests verbatim, dead-spine
  pass (your own rule: it is not optional), `## STATE`, run_record envelope.
- The LIVE spine (port 8090) and dev preview (port 3000) are production —
  read-only for you. Second dev server on 3100 as before; do NOT run
  `next build` while any dev server lives (your recorded hazard).

## Part A — money-adjacent safety (ClarkHarness; validator recs, accepted)

A1. **Rebalance attribution guard**: `RebalanceService.build()`/`_composition`
    must refuse any strategy whose folded per-strategy position disagrees
    with the authoritative NAV position fold for that symbol (the general
    fix — catches every future mistagged fill), and must refuse archived
    strategies as targets. Measured incident to pin in tests: the phantom
    ±$174.47 GLD pair produced a $376.84 BUY with `current_usd: 0.0` and
    zero warnings (docs/AUDIT_R6_D2_ATTRIBUTION_2026-08-20.md, ITEM 3).
A2. **Two-sided strategy cap**: `riskmonitor.py:511` uses `weight >
    strat_limit`, so a negative (phantom-short) strategy weight can never
    breach. Make it `abs(weight)`, message stating the sign.
A3. **Attribution repair mechanism** (mechanism only — the CTO fires the
    actual event after merge): a `StrategyAttributionCorrected` event type
    + fold support in `projections/strategy.py`, moving qty at cost between
    strategy ledgers with a mandatory written reason in the payload. Log
    stays append-only; the correction is an event, never a rewrite.

## Part B — instrument plumbing (ClarkHarness + UI; validator + CDO, accepted)

B1. **`venue` on OrderFilled**: pipeline records venue on the fill event
    payload (it already knows it from the submitted leg). TCA gains a
    `by_symbol` cut and a venue split at the STATS level — `/fund/tca`'s
    execution/total blocks currently average paper-venue zeros into
    Monitor's EXECUTION QUALITY panel (CDO D3, CTO-confirmed). The
    vs_assumption verdict was already fixed (tca.py) — extend the same
    "informative fills only" discipline to the stats the panel quotes, and
    the panel labels which venue's fills it counts.
B2. **`money_at_stake` on recommendations**: optional float on the desk
    run recommendation shape (POST /fund/desk/runs), carried through the
    fold and rendered by the CEO desk's ranking (execDesk.ts already ranks
    null-last and prints the gap honestly — keep that behavior when the
    field is absent).
B3. **Machine-checkable review triggers**: `Judgement` gains an optional
    declarative `trigger_spec` (metric source key + comparator + value —
    e.g. `risk_monitor.max_drawdown_pct > 3.0`) evaluated in
    `due_for_review` alongside the date. Migrate the quantifiable entries
    (min_effective_bets' drawdown trigger at minimum); free-text triggers
    stay as prose. Pin with a test that a fired trigger surfaces in
    `/fund/judgement` `due_for_review` — the defect was `due_for_review:
    []` while a 7.75% drawdown sat in plain sight.
B4. **Desk load counter for the COO rule**: the CEO's standing rule is a
    COO triage dispatch when OPEN items on the CEO's desk exceed 20 (open
    recommendations + pending orders + requests awaiting approval). Expose
    the count (spine, wherever the desk fold already lives), render it on
    the CEO desk and CTO console with a "COO triage due" chip when >20.
    The chip is a signal for the CTO to dispatch — it triggers nothing
    itself.

## Part C — halt classes (ClarkHarness + Monitor UI; CEO-blessed principle)

C1. **Two halt classes**: `integrity` (bad/absent mark, stale feed, dead
    heartbeat — resumes only on integrity restored + human ack) and `loss`
    (drawdown, daily-loss — circuit breaker with a reopening procedure).
    Classify existing halt causes; carry the class on the halt event and
    through `/fund/risk/monitor` so the UI can say which kind of dark this
    is. Manual resume stays for both; sells-allowed asymmetry stays.
C2. **Acknowledge-and-rebase** (loss class only): a CEO-only action that
    rebases the daily-loss reference to current NAV with a mandatory
    written reason, recorded as an event (`LossReferenceRebased`), and
    REFUSED while the cause is an open integrity halt. It goes through the
    approval-channel guard exactly like an order approval (allowlist
    neelesh/neelesh-via-cto, confirm echo, via-cto requires the quoted
    instruction; refusals recorded). Riskmonitor's daily-loss alarm reads
    the rebased reference. UI: the Monitor kill-switch panel gains the
    control with the guard's confirm supplied from the rendered state.
C3. **F4 latency**: the daily-loss halt took 14m41s (~29 ticks) to fire
    after breach (riskofficer F4, accepted). Find why in the monitor tick
    path, fix so a breach halts within ONE tick, and add a test measuring
    tick-to-halt on a simulated breach. If the cause turns out to be
    something a test cannot pin, say so and report the measurement instead.

## Part D — CDO fix batch (KryptonPay; CDO recs, accepted)

D1. Clark rail defaults CLOSED below the push breakpoint
    (`ClarkConsole.tsx:69`, overlay at `:367`) — pill only on mobile.
D2. Allocate's AT WORK hero never renders 0.0% while archived strategies
    hold exposure — true book number or an absent dash with the banner's
    sentence in the hero position.
D4. Split "awaiting your decision" from "decided, awaiting execution" on
    the CEO desk and seat pages; headline counts ONLY the former.
D5. Wordmark: "Krypton Fund" (`StudioHeader.tsx:39`).
D6. Delete the dead `obsidian.css` import (`globals.css:5`) and the file
    (zero consumers — verify with your own grep before deleting).
D8. Denominators on the surface: ticker held-weight badge says "of NAV";
    BOOK SHAPE rows carry "of invested capital" (lift from footnote).
D9. Monitor states the halt once as the anchor banner + the kill-switch
    panel it links to; RiskBar keeps repeating everywhere (its charter).
    Remove the redundant restatements only — do not weaken any signal.
D10. The wire section states its absence ("the wire could not be read" /
    "no interactions recorded yet" — distinguish unreadable from empty)
    instead of vanishing (`desk/page.tsx:234`).

## Explicitly OUT of scope

- CDO D7 (emerald scope) and the prefers-color-scheme decision — both are
  design-constitution VERSIONING decisions awaiting the CEO; touch nothing.
- The 3D floor — its own dispatch.
- Abhishek's surfaces (`thesis/**`, his types in fund_api.ts), gate/
  autopolicy/risk THRESHOLD VALUES (the halt-class work changes machinery,
  never a number), the live trees, the running servers.
- Firing any event at the live spine — you build mechanisms; the CTO fires
  events after merge.

Every part lands with tests; parts that touch money paths (A, C2) get the
densest ones. If a part cannot be completed honestly in this dispatch, ship
the parts that can and name what you did not do — a stated gap beats a
rushed money-path change.
