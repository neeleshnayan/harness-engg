# Builder dispatch 6 — Lab analytics, merge gate, factor pack, halt controls

**2026-08-21. CEO-accepted scope (run-builder-dispatch5 recommendations,
all five accepted 06:41-06:43Z, cascade executed by the CTO). Isolated
worktree as always. Read `.claude/state/builder.md` FIRST.**

## Worktree bases — VERIFY BEFORE ANY WORK (wrong 5/5 dispatches)

- ClarkHarness: branch `claude/krypton-fund-agentic-j8r2mu`, expected
  `git log -1` = `d7a04bf` "Approval guard v1.2: neelesh-via-co-cto
  identity (CEO decision 2026-08-21)".
- KryptonPay: branch `claude/krypton-fund-agentic-j8r2mu`, expected
  `git log -1` = `21be00b` "Merge branch 'builder-d5'".
- If either base disagrees: use your clone-both recovery immediately —
  do not build on a wrong base. `git -C` into the shared checkouts is
  refused by the guard; clone first, verify inside the clone with
  `git merge-base --is-ancestor <base> HEAD`.

## Part G — Lab analytics (THE HEADLINE; CEO's words, three times over)

> "the quant page aka lab has no analytics making it hard for me to
> understand agents runs... unify the experience so I can validate agents
> runs same way i would mine" / "importantly be able to see the analytics
> behind the runs!" / today: "i can see monthend_rebalance_flow but cant
> see the analytics behind like discussed"

The engine computes per-run analytics; the factory DROPS the payload
before storage (your own D5 finding — this is why nothing renders). Scope:

1. Spine: persist the engine result payload per job (equity curve, trade
   list, per-fold walkforward figures, gate inputs) — Postgres-mirrored
   like parsed results, pruned by the existing results-prune tick policy.
2. API: serve it on the existing job/candidate read paths (additive
   fields only — no breaking shape changes; check what the Lab already
   consumes before renaming anything).
3. Lab UI: analytics panel per run — equity curve, folds table with
   `measurable`/`dates_honoured` flags surfaced honestly (an unmeasurable
   fold renders as unmeasurable with its named reason, NEVER as zero),
   cost-sweep verdict band, and the gate's sentence verbatim.
4. Unify: the same run-record envelope the desk uses (`/fund/desk/runs`)
   is the Lab's index — one validation experience, desk and Lab.

Absence rule binds hard here: a missing figure renders as absent, never 0.

## Part C — merge gate script

`ClarkHarness/scripts/merge_builder.py`: takes a bundle path + expected
base; verifies base ancestry, applies to a temp clone, runs the full suite,
checks the forbidden-surface list (guard/autopolicy/gate/riskengine/
exitrule/events store, Abhishek's surfaces), emits a PASS/FAIL report the
CTO (or co-CTO, per the new charter) reads before merging. It GATES a
merge; it never performs one.

## Part D — factor pack v0

`app/fund/factors.py`: daily factor return series from ETF proxies on the
EXISTING feed (mkt=SPY, duration=TLT, commodity=DBC, dollar=UUP if
available — check the feed, do not assume), rolling betas + residual
helper for analyst/adversary use. Pure read/compute, no thresholds. The
SRPT revival condition (beta-adjusted residual vs XBI/IBB) is the first
consumer — include XBI/IBB if the feed serves them.

## KryptonPay items

5. Halt-control UI: acknowledge + drawdown-rebase controls on the risk
   monitor page. The payload already serves halt_acknowledgement /
   halt_alarm / halt_ack_token / autoresume_cooldown_minutes and
   drawdown.{peak_basis, peak_note, unrebased_peak_nav, rebase,
   rebase_token}; nothing consumes them yet. Approve-channel calls need
   confirm echo = the served token; approver identity is typed by the
   human — NEVER prefilled to an allowlisted identity.
6. Live-vs-backtest panel (Monitor): filter archived strategies out by
   default with an "include archived" toggle — the CEO sees dead
   strategies presented as live comparisons today.
7. Bench panel: same stale-data cleanup (archived filtered, absent
   numbers shown absent).
8. faces.ts: map `neelesh-via-co-cto [...]` to the cto face (one regex
   line; a distinct co-cto face is NOT in scope).

## Quant harness items (accepted from run-quant-entry11)

9. LEAN_JOB_TIMEOUT: raise + serialise queue behaviour per the quant's
   report; a timeout must be reported as TIMEOUT, never as "no trades".
10. `walkforward.folds` in the candidate API response (requested dates +
    `dates_honoured`).
11. Fractional-fills belt config: an algorithm-level or config-level
    switch enabling fractional share fills in backtests (the venue
    supports fractional; the whole-share default is the ENGINE's, and it
    blocked entry-11's honest re-measure at $2k NAV).
12. Split "timeout" from "no trades" in the unmeasurable-fold reasons.

## Register note

13. The 30-min auto-resume cooldown was CEO-accepted with your proposed
    basis ("one STRIKE_INTERVAL, measured from the acknowledgement") —
    if judgement.py's register entry does not carry that basis note yet,
    add it (additive text, no value change).
    NEW_SYMBOL_WITHOUT_REFERENCE_REFUSES stays False (CEO-accepted).

## Boundaries and pace

Out of reach as always: live tree, running spine, gate/autopolicy/risk
VALUES, Abhishek's surfaces (`app/fund/thesis_generator/**`,
`src/app/clark/studio/thesis/**`, his types in `fund_api.ts`). G is the
largest and the priority: if the dispatch must stop at a sanctioned
boundary, deliver G complete over starting everything — a two-dispatch
feature built well costs less than one built twice. Zero new
dependencies unless named in your report with the reason. Full suites
green on both merged trees; CDP-probe every UI change (your own
doctrine: measure, don't eyeball). End with `## STATE`.
