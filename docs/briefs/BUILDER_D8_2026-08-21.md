# Builder dispatch 8 — the day's defects, in priority order

**2026-08-21, dispatched by the co-CTO chair. Nine parts, all CEO-instructed
or CEO-accepted, drained from the desk into one brief per the batch-by-seat
rule. PRIORITY IS THE ORDER GIVEN — stop at a sanctioned boundary with
earlier parts complete and tested rather than starting everything.**

## Worktree bases — VERIFY FIRST (wrong 6/7 dispatches historically; D7 was the first correct one)

- ClarkHarness: branch `claude/krypton-fund-agentic-j8r2mu`, expected
  `git log -1` = `28a6c28` "PM divergence memo filed verbatim". Note an
  untracked `.env.backup-2026-08-21-preflag` exists in that tree — leave it.
- KryptonPay: branch `claude/krypton-fund-agentic-j8r2mu`, expected
  `git log -1` = `a3d59de1` "Dispatch briefs D6 and D7 committed".
- Your D7 merges are in both histories. Clone-both recovery if either
  disagrees; `git -C` on the shared checkouts is refused by the guard.

## Context you must read before Part A

Today's chain, because six of these parts come from it:
`.claude/state/CTO_REVIEW_QUEUE.md` (the co-CTO ledger),
`docs/AUDIT_RISKOFFICER_2_2026-08-21.md`, `docs/pm/PM_DIVERGENCE_2026-08-21.md`.
Short version: `USE_FAKE_FIRESTORE=1` was one flag doing three unrelated
jobs. It made the offsite durability mirror in-memory (552 events lived on
one disk while the status endpoint reported success hourly), it routed every
order to an internal paper connector (so the CEO-authorised cost experiment
produced nothing), and it labelled the spine "NOT the fund" while it held
four real positions. Durability is restored and verified; the rest is here.

---

## PART 1 (HIGHEST) — TCA correctness. Two small fixes that gate a real experiment.

**1a. `tca.py:212` must resolve venue from the SUBMITTED leg, not the fill
label.** Today it reads `f_pay.get("venue") or s_pay.get("venue")` — the
self-declared `OrderFilled` label first. That field is copied from the
proposal and is forgeable; the `OrderSubmitted` payload carries what the
connector actually returned. Measured contamination right now: today's DBA
order (proposed `alpaca`, **submitted `paper` at seq 593**, filled-label
`alpaca` at seq 594) is counted as informative, moving the informative set
n=8→9 and the fund's headline realised cost **5.56bps → 4.95bps** — exactly
the tautology `tca.py:281-284`'s own comment says the paper exclusion exists
to prevent. This gates R15 (the execution-cost experiment must not be re-run
until it lands).

**1b. `/fund/tca` silently truncates, oldest-first.** `fund.py:516` defaults
`limit=500` and passes it to `costs(limit)` → `_lifecycles(limit)` →
`store.stream(limit=...)`, and `pgstore.py:259-277` is
`WHERE seq > %s ORDER BY seq ASC LIMIT %s` — **the oldest n events**. The log
holds 725, so both of today's fills are invisible on the default read (20
orders vs 22 at `?limit=5000`), and the fraction lost grows every day. Fix
so the limit means what a caller expects (most recent orders), and if a
truncation still occurs, SAY SO in the payload — a silent under-report of
the fund's own cost history is the absence-is-not-zero rule broken.

## PART 2 — Make broker-book drift detectable (it currently is not)

**2a. Reconciliation liveness heartbeat, THREE states.** Reconciliation is
absent from `/fund/liveness` entirely. Worse, `main.py:257-262` wraps
`run_reconcile()` in a try/except that logs a warning, then beats
`nav_strike` unconditionally at `:262` — so a dead reconciler reads green.
States: `ran (N symbols checked, M structural)` / `skipped: <reason>` /
absent past budget. **The exact pattern already exists eleven lines above at
`main.py:250-254`** where `nav_strike` beats with `note=f"no strike —
{reason}"` so "market closed" cannot read as "the loop died". Copy it.

**2b. Broker-book drift alarm on the risk monitor**, specified by the PM
(`PM_DIVERGENCE_2026-08-21.md` §5.1) — implement as specified, do not
re-derive:
- **Primary: per-symbol QUANTITY** at `_TOL = 1e-6` (`reconcile.py:20`),
  netting out (i) `OrderSubmitted` with no terminal event, (ii) corporate
  actions (`positions.py:108-113`), (iii) the Part 3 epoch baseline. **No
  dollar band on this leg** — a quantity that still disagrees after netting
  IS an order that did or did not happen.
- **Secondary, info-only: NAV-equity band `max(1.0% of NAV, $150)`.** The
  1.0% comes from a measured mark-timing residual of $0.92 = 0.049% of NAV
  (~20× the noise floor); the $150 is the smallest live position (DBA
  $149.94). Legs cross at NAV $15,000. A dollar band cannot be primary
  because `max_order_notional_pct` is 15% = $282.96, so any band under $283
  fires on every legitimate in-flight order.
- Severities: structural drift → `warn`; structural drift on a symbol with
  an armed exit rule → `critical`; NAV band alone → `info`.

**DO NOT touch `app/fund/autopolicy.py`.** The envelope change that consumes
this alarm (the venue must hold what an exit rule sells) is the riskofficer's
lane and the CEO's decision — request `86f7662e`. Your job is to make the
signal exist, not to act on it.

## PART 3 — Fence the divergence cohort (PM R18, Clean Field Rule)

One dated epoch record naming the ten out-of-sync symbols and their
quantities as of **2026-08-21T15:42:35Z**, preserved beside the live
comparison and never erased, with `Reconciler.run()` netting it out so only
POST-epoch drift raises a `ReconciliationMismatch`. Rationale: the broker's
position book is a pre-instrument artifact (a photograph of a fund whose
orders never routed) and cannot be re-baselined without an instrument the
fund must not build — so the constitution's unrecoverable-measurement branch
applies: fence it, don't launder it. Measured urgency: the reconciler is
currently emitting **~178 `ReconciliationMismatch` events/day** into a
725-event log, which is the exact failure `reconcile.py:96-99` documents
against itself ("or the audit trail trains its readers to ignore it").

## PART 4 — Remove `USE_FAKE_FIRESTORE` entirely (CEO instruction)

CEO verbatim: *"no more of fake firestore, everything is served off postgres
and persistent"*. Call sites: `app/api/v1/fund.py` (128-129 `_mock_mode`,
156 connector selection, 646 `seeder_may_run`), `app/core/firebase.py`
(42, 66, 81-87), `app/main.py` (43-45, 77), and the scripts that set or
document it (`finish_promotion`, `live_seed`, `mock_seed`,
`promote_local_to_production`, `sync_local_to_production`).

**TWO THINGS THAT MUST SURVIVE THE DELETION:**
1. **`firebase.py:81-87`'s refusal** — it stops a standalone repair script
   run with `--apply` from writing to the real append-only ledger while
   believing it is in mock mode. Re-express it against whatever replaces the
   flag (an explicit offline/dry-run switch), do **not** drop the safety
   with the flag.
2. **Order routing must become its own flag.** That is the actual lesson:
   one flag governed store target, order routing and ledger identity.
   `FUND_REAL_BROKER` already exists because someone split one of the three
   off before — finish the job, three orthogonal names, each meaning exactly
   what it says.

**Also: the service-account default points at the WRONG project.** `.env`
sets `FIREBASE_SERVICE_ACCOUNT_JSON=firebase_service_account.hedgefund.json`
(**hedgefund-ae96c**, the fund) while the code default is
`firebase_service_account.json` (**krypton-auth-e8653**, a stale auth project
that ALSO contains a `fund_events` collection). Any script not loading
`.env` silently targets the wrong project — this bit the co-CTO's own probe
today. Make the fund project explicit and the wrong one unreachable by
accident.

## PART 5 — `/fund/book` tells two stories (PM R25 is the CEO's decision; this is the reporting half)

`/fund/book` currently returns `venue: "alpaca"` and `orders_are_real: false`
in the same object, against that endpoint's own docstring
(`fund.py:638-639`: *"'mock' must never hide that real orders are leaving the
building"*). `_real_broker()` (`fund.py:140-141`) requires
`FUND_REAL_BROKER`, absent from `.env`; connector selection does not. **Do
not decide the flag** — that is R25, a human call. Do make the payload stop
contradicting itself: report what the connector actually IS, derived from
`_connector`, and keep any separate "this env var is set" fact under its own
distinct key.

## PART 6 — Donna's memo on the CEO desk (CEO instruction, request 920ecbe5)

CEO verbatim: *"For Donna http://localhost:3000/clark/studio/desk/ceo lets
have her high level memo for today from her yesterdays EoD and when it
arrives autoupdate it"*. The Donna queue currently renders an empty state.
It must surface her LATEST filed Daily's high-level memo (the TL;DR plus §1
THE DAILY: book line, what moved, awaiting-you) with its **date visible** so
yesterday's memo is visibly yesterday's, auto-updating when a newer one is
filed. `GET /fund/desk/archives` already serves the index; serving §1's
CONTENT needs either a new spine read endpoint or an extension — your call,
but the browser must not read the filesystem. The long record stays on her
seat page as work output, never a CEO-desk item. Keep the three absences
distinct: never-filed vs unreadable vs none-yet.

## PART 7 — Dispatch needs a THIRD state, and explicitly NOT an auto-close (request 907ecc74)

CEO verbatim: *"no it should nto close automatically since the cto needs to
review the work be satisified and then log or do what needs to be done and
then close it"*. Today three finished dispatches rendered as WORKING for
hours. `DeskDispatched` mints a `task_id` and only a resolution carrying THAT
id closes it. The floor renders two states where there are three: **WORKING**
(dispatched, seat running), **AWAITING REVIEW** (seat returned, run recorded,
nothing verified or filed yet), **CLOSED** (chair reviewed, acted, resolved).
Build the middle state and render it distinctly on the floor and the CTO
console — a returned-but-unreviewed dispatch must read as an obligation, not
a busy seat. Detection signal: a `fund_agent_runs` row exists whose
trace/run matches the dispatch while no resolution for the task_id does.
**DO NOT auto-close.** Closing is the chair's judgement step; removing it
would report a completion nobody performed.

## PART 8 — `noted` is missing from the recommendation vocabulary

The constitution gives the secretary two kinds — `note` (asks to be READ, no
accept/reject, the chair marks it noted) and `suggestion` (decidable). But
`decide_recommendation` accepts only `open|accepted|rejected|staged|done`,
so both chairs have been marking notes `done`, which conflates "read, nothing
required" with "executed". Add `noted` as a terminal status, rendered
read-only. Small, and it makes the CEO's own vocabulary real.

## PART 9 (LOWEST) — the untouched Part G addendum from D7

Only if the above are complete and green: `GET /fund/autopolicy` (read-only
envelope + version + constants with registered basis; no write path ever);
ClarkHarness H1/H3 (bar date on every mark); the eight named spine gaps from
dispatch 1 (additive read surfaces only — list any needing a decision and
skip them); `scripts/day_pack.py` and `scripts/resolve_run.py` (the latter
must NOT write to `.claude/`); `DECISIONS_PER_TEST_LEG` on walkforward fold
rows.

---

## Boundaries

Unchanged: no gate/autopolicy/risk VALUES, **no `autopolicy.py` at all this
dispatch**, no Abhishek surfaces (`app/fund/thesis_generator/**`,
`src/app/clark/studio/thesis/**`, his types in `fund_api.ts`), no live-tree
writes, full suites green on both trees, zero new dependencies unless named
with a reason, CDP-probe every UI change. Measure test counts immediately
before writing the commit message — that has been wrong two dispatches
running, and the merge gate's verbatim suite tail is the authority. End with
`## STATE`.
