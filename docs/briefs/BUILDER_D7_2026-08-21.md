# Builder dispatch 7 — CEO desk v2, Donna's presence, interaction durability, research-data fixes

**2026-08-21. Continues the same builder conversation from D6 (CEO-approved
pattern: same seat, sequential briefs, merge between, one run record each).
CEO-approved scope: desk request 23b075a6 (approved under the CEO's quoted
instruction) + the CEO's same-day amendments + adversary r4 rec 4 +
analyst cycle-2 defect fixes + D6 deferrals that need no new decision.**

## Worktree bases — VERIFY FIRST (wrong 6/6 dispatches; clone-both immediately)

- ClarkHarness: branch `claude/krypton-fund-agentic-j8r2mu`, expected
  `git log -1` = `ec816f7` "Analyst cycle 2 filed verbatim". Your D6 merge
  is in this history.
- KryptonPay: branch `claude/krypton-fund-agentic-j8r2mu`, expected
  `git log -1` = `cbc32b8a` "Merge builder-d6". NOTE: `components.tsx`
  gained a CTO hot-fix mid-D6 (compact seat cards render ONE ×N runs chip;
  dashed ×? for unmeasured; non-compact unchanged) — read it before
  touching that file.

## Part A — THE CEO DESK BECOMES FOUR QUEUES (the headline)

CEO verbatim: "my desk should have 4 queues -> Vishesh, Donna, Fable and
Others segregated by team member name."

1. **VISHESH queue**: COO batch memos and triage items.
2. **DONNA queue**: the EoD Daily lands here as a CARD the way the COO's
   memos do — the sixty-second read (TL;DR + ranked awaiting-you list),
   full record ONE CLICK away on her seat page. AMENDED BY CEO: only the
   SHORT memo lands the desk; the long record is her seat-page work
   output, never a desk item. Her items carry kind `note` or `suggestion`
   (secretary.md): notes render WITHOUT accept/reject (read-only; the CTO
   marks them noted); only suggestions get decision buttons.
3. **FABLE queue**: CTO-staged orders, seat-filed asks awaiting the CEO
   (your D6 ask surface moves in here), decisions routed up — with the
   four-state lifecycle (open/approved/resolved/declined) and both
   approve + decline controls (decline prompts for the mandatory reason).
4. **OTHERS**: everything else, grouped by seat name.

## Part B — Donna's office presence

1. Secretary desk on the 2.5D floor, executive row beside Vishesh
   (constitution: the seat carries the name Donna); seat page at
   /desk/secretary via seatLib.
2. Her seat page lists every archived Daily over time: index of
   `ClarkHarness/docs/archives/*.md` (serve via a small spine endpoint —
   the UI must not read the filesystem), newest first, PDF link beside
   each when present.

## Part C — floor run counts

CEO: "the floor doesnt capture how many runs each agent had that day."
Per-desk runs-today numeral visible ON the room view itself (today it
only renders in the click-open detail, floor/page.tsx:163). Match the
×N chip style from the CTO's components.tsx hot-fix. Humans and fixtures
keep their no-count honesty lines.

## Part D — interaction durability (CEO decision 2026-08-21)

1. Table `fund_agent_transcripts` (run_id, kind: brief|report|transcript,
   content TEXT, created_at) + POST/GET under
   /fund/desk/runs/{run_id}/transcript; an ingest script that lifts a
   session task JSONL into it. NO retention policy — cleanup is a later
   versioned decision, the CEO said so explicitly.
2. Extend FirestoreSnapshotter to mirror `fund_agent_runs` (the flight
   recorder is currently single-copy — CTO finding 2026-08-21) on the
   same hourly cadence, with the same behind-by accounting in
   /fund/snapshot/status.

## Part E — research-data fixes (analyst cycle 2, all confirmed by measurement)

1. `fund_observations` gains `accepted_at TIMESTAMPTZ` + `period DATE`,
   populated from EDGAR `acceptanceDateTime` and `reportDate`
   (edgar.py:138-139 currently discards both; they are present on 201/201
   tickers). CRITICAL detail the analyst verified: acceptanceDateTime
   carries a "Z" suffix but is ET = the stamp minus 4 hours — store it
   normalized to real UTC and SAY SO in the column comment. Backfill
   existing observations from EDGAR where the accession is in the stored
   URL. This closes a 55.9% sub-daily lookahead.
2. The 8-K reader follows the filing index to exhibit **EX-99.1** instead
   of `primaryDocument` (edgar.py:86-89) — 83% of 8-K reads are zero-yield
   cover pages; item 2.02 earnings content is unreachable until this.
   Also store the 8-K `items` codes (free on 12,164/12,164).
3. Route equity-namespace symbols away from `_crypto_id` when an EDGAR
   CIK exists (BTC = Grayscale Bitcoin Mini Trust, not bitcoin spot);
   keep a bar-count integrity guard.

## Part F — adversary r4 rec 4 (the gate data path; CEO-accepted)

Persist aligned DAILY strategy + benchmark return series per fold and for
the holdout, ONE feed, undownsampled, on the candidate's analytics (your
D6 JSONB column is the natural home — a `daily_returns` key per leg).
This is the prerequisite for gate v5 round 5; without it no premia
statistic is computable. Do NOT touch gate.py — capture the data only.

## Boundaries

Same as always: no gate/autopolicy/risk VALUES, no Abhishek surfaces, no
live-tree writes, full suites green, zero new dependencies unless named,
CDP-probe every UI change. Priority order: A, B, D, E, C, F. Stop at the
sanctioned boundary rather than rushing the tail. End with ## STATE.
NOT in scope (decisions pending): belt `queued` state, gate.py holdout
split, the hardcoded "neelesh" approver convention, the COO trigger
threshold.
